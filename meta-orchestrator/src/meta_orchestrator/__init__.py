"""Learning Meta-Orchestrator — Phase 1 MVP.

See ``SPEC.md`` (source of truth) and ``BUILD-CHECKLIST.md`` (execution engine).
Phase 1 deliberately builds only the [P1] parts: taxonomy, persistence + Registry
with provenance, verification+memory backbone, Decision Engine v1, bandit learning,
a single-agent LangGraph loop, and an eval harness that proves learning.
"""

__version__ = "0.1.0"
