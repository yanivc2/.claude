{
  "name": "ap-control",
  "version": "0.1.0",
  "private": true,
  "description": "מערכת בקרת ספקים, חשבוניות ותשלומים (Accounts Payable control) — שלב 1",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/db/seed.js",
    "reset-db": "node src/db/reset.js",
    "scrape": "node src/scraper/run.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "ejs": "^6.0.1",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  }
}
