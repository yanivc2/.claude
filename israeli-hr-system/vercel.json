{
  "buildCommand": "(prisma db push || (sleep 15 && prisma db push) || (sleep 30 && prisma db push) || echo 'prisma db push skipped after retries — continuing build') && next build",
  "crons": [
    {
      "path": "/api/cron/legal-updates",
      "schedule": "0 6 * * 1"
    },
    {
      "path": "/api/cron/retention",
      "schedule": "0 7 * * *"
    }
  ]
}
