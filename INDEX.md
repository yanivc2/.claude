# INDEX — מפת פיצ'רים, כפתורים והשפעות (AP Control)

**מטרת הקובץ:** לפני **כל** שינוי — קראו את החלק הרלוונטי כאן. לכל פיצ'ר/כפתור מתועד: מה הוא עושה,
לאן הוא מחווט (route → service → view), למה הוא מקושר, **ומה קורה אם מוחקים / משנים / מזיזים אותו.**
המטרה: שאף שינוי לא ישבור משהו אחר בשקט.

> **פרוטוקול שינוי (חובה):**
> 1. מצא את הפיצ'ר בטבלה המתאימה למטה → קרא את עמודת "אם מוחקים/משנים/מזיזים".
> 2. עבור על **"אינווריאנטים נושאי-עומס"** (הפרק הבא) — אלה הדברים שנשברים בשקט.
> 3. הרץ `npm test` **וגם** `TEST_PG=1 npm test` (שני הדיאלקטים) + `node scripts/smoke.mjs` לפני דחיפה.
> 4. שינוי סכימה? עדכן 3 מקומות (ראה למטה) והזכר לבעלים ללחוץ **הגדרות → "עדכן מסד נתונים"**.
> 5. עדכן את הקובץ הזה **ואת `CLAUDE.md`** כשמוסיפים/משנים פיצ'ר.

---

## 🔴 אינווריאנטים נושאי-עומס (נשברים בשקט — קרא לפני הכל)

| # | האינווריאנט | איפה | אם שוברים אותו |
|---|---|---|---|
| 1 | **סכימה חיה ב-3 מקומות** | `src/db/schema.sql` (SQLite חדש), `src/db/migrate.js` (SQLite קיים), `src/db/schema.pg.sql` (Postgres, כולל `ALTER … ADD COLUMN IF NOT EXISTS` בתחתית) | עמודה/טבלה חדשה שלא נוספה לכל 3 → קורס בפרוד או ב-CI; שדה יוצא `undefined` בתצוגה. |
| 2 | **`RETURNING id` אוטומטי ב-PG** | `src/db/adapter.js` — כל `x.run('INSERT…')` מוסיף `RETURNING id`. | טבלה שה-PK שלה **אינו `id`** (למשל `app_settings.key`, `invoice_ocr.invoice_id`) → ה-INSERT קורס ב-Postgres. **פתרון:** הוסף `RETURNING <pk>` מפורש ל-SQL. |
| 3 | **`x.one` מחזיר `undefined`** (לא `null`) כשאין שורה | `adapter.js` | לבדוק עם `!row`, לא `=== null`. |
| 4 | **כסף = אגורות שלמות** (₪1 = 100) | `lib/money.js` | חישוב בשקלים/צף → סטייה ב-R5 (סכום צ׳ק = Σ שורות). תמיד `toAgorot`/`fromAgorot`/`formatIls`. |
| 5 | **חומת הרשאות (firewall)** | `middleware/requireOwner.js#enforcePageScope` + `lib/permissions.js` (`NAV_PAGES`,`NAV_ALLOW`,`OPEN_PATHS`) | תפקיד מוגבל (קופאי→`/zclosing` בלבד) יגיע לכל דף אם משנים לא נכון. נתיב חדש שצריך גישה → הוסף ל-`NAV_ALLOW`/`OPEN_PATHS`. |
| 6 | **`footer.ejs` — enhancers גלובליים** | `partials/footer.ejs` | בורר-תאריך מחליף כל `input[type=date]` (חשוף כ-`window.apEnhanceDates(root)` לשורות שנוספות דינמית, כמו הוצאות סגירת-Z); קומבובוקס מחליף `<select class="js-combo">`; **מקפל רובריקות** — כל `.card` עם `<h2>` ישיר הופך למתקפל (אקורדיון, נשמר ב-localStorage; דף יכול לבקש שכל הכרטיסים המתקפלים יתחילו **מצומצמים** ע"י `[data-collapse-default]` בתוך `<main>` — כמו דף ההגדרות — אך בחירה מפורשת של המשתמש שנשמרה גוברת); **כלי-טבלה** — כל `<table>` עם `<thead>` ו-≥2 שורות מקבל חיפוש חי (מסנן כל העמודות בכל הקשה) + **כפתור "ייצוא" יחיד** (`.tbl-export-toggle`) שפותח תפריט (`.tbl-export-menu`, מוסתר עם `[hidden]`) עם Excel/CSV/PDF של השורות הנראות (עמודות `.no-print`/`[data-noexport]` **מוחרגות** מהייצוא). דילוגים: `.no-collapse`/`.no-enhance`, `<details>`, כרטיס עם `[required]`. גם **`window.apEnhanceCombos(root)`** — חושף את מַשְׁפֵּר הקומבובוקס לשורות שנוספות דינמית (בורר העובד בהוצאות מזומן), במקביל ל-`apEnhanceDates`. גם **`window.apAutoSubmit(el)`** — עוזר גלובלי לשליחת טופס מ-`onchange` (בורר חנות פעילה בבאנר, מסנני מרקורים/דוחות/התאמה) **בלי submit תוכנתי** (בתוך PWA מותקן ב-iOS, submit תוכנתי של POST שמחזיר 302 "בורח" ל-Safari — "address is invalid"): טופס **GET** → בונה `action?params` ו-`location.assign` (נשאר באפליקציה); טופס **POST** → `fetch()` ואז ניווט צד-לקוח ל-`return_to`. השרת עונה ל-fetch (כותרת `X-Requested-With: fetch`) ב-**204** במקום redirect (`routes/context.js#/store`) — post רגיל (ללא JS) עדיין מקבל 302 (fallback). כל בורר עם `onchange="apAutoSubmit(this)"` תלוי בו. שינוי שם משפיע על **כל** הדפים. |
| 7 | **`BUILD_VERSION`** ב-`src/app.js` | מוצג ב-footer של דף login | לא מקדמים כל deploy → אי אפשר לאמת שהעלייה חיה. סשנים מקבילים מתנגשים על אותו `·NN` — קח מספר חדש אחרי rebase. |
| 8 | **DB חי = Postgres (Neon); אין apply-schema אוטומטי** | `api/index.js` `connectDb()` | אחרי שינוי סכימה הבעלים **חייב** ללחוץ הגדרות → "עדכן מסד נתונים". אין לי גישה ל-DB החי — deploy לא נוגע בשורות קיימות. |
| 9 | **workflow דחיפה** | `apnew/main` = מה ש-Vercel מפרסם | `git push apnew ap-control-split:main` + mirror `git push origin ap-control-split`. `fetch apnew main && rebase` לפני כל דחיפה. אף פעם לא לדרוס commit של סשן אחר. |
| 10 | **סכימה נבחרת מפורשות ב-SELECT** | הרבה services בוחרים רשימת עמודות מפורשת (`getUser`/`listUsers`) | עמודה חדשה שלא נוספה ל-SELECT → `undefined` בתצוגה למרות שהיא במסד. |
| 10.5 | **GET לכתובת-פעולה לא מגיע לדף שגיאה** | `middleware/actionUrlFallback.js`, מותקן פעמיים ב-`app.js`: `'early'` אחרי `enforcePageScope` ולפני ה-routers, `'late'` לפני ה-404 | טבלת ה-routes נקראת **מ-`app._router.stack`** (lazy, בבקשה הראשונה) | **הבאג:** לא מעט handlers של POST עונים ב-**render** של דף הרשימה במקום ב-redirect אליו. הפעולה מצליחה, אבל הדפדפן נשאר על כתובת שמקבלת רק POST — ורענון / חזור-קדימה / שחזור PWA שולחים GET ל-`/employees/54/stores` ומקבלים "הדף המבוקש לא נמצא", כשהשמירה כבר בוצעה. ה-middleware מזהה GET לנתיב שרשום רק ל-POST ושולח ל**אב הקרוב ביותר שכן עונה ל-GET**. `'early'` מטפל רק ב**נתיב ליטרלי שנתיב-פרמטר מסתיר** (`/invoices/pay-batch` מתחת ל-`/invoices/:id`, שמנסה לטעון חשבונית בשם "pay-batch" ומחזיר "הרשומה לא נמצאה") — כל השאר ב-`'late'`, **אחרי** שומרי ההרשאות, כדי שמשתמש לא-מורשה עדיין יקבל 403 ולא redirect ידידותי. `/ingest/*` מוחרג (endpoints של מכונה — GY נודד חייב להישאר 404). | **זו רשת ביטחון, לא היתר:** handler חדש עדיין צריך לעשות redirect 303 אחרי הפעולה (PRG). `test/action-url-fallback.test.js` סורק את **כל** ה-routes שרשומים רק ל-POST ודורש 303 — route חדש מכוסה אוטומטית. הסרת ה-`'early'` מחזירה את "הרשומה לא נמצאה" על `/invoices/pay-batch` ודומיו. |
| 11 | **הקשר "חנות פעילה" + הרשאה פר-חנות** | `middleware/currentUser.js` (קובע `req.activeStoreId`, **מצמצם את `req.scope` לחנות הפעילה** ושומר את ההרשאות המלאות ב-`req.grantedScope`, `res.locals.activeStore/availableStores`), `lib/scope.js` (`authorizedStoreIds`/`availableStoresFor`/`setUserStores`/`effectiveStoreId`/`assignmentScope`), `routes/context.js` (`POST /context/store`, cookie `ap_store`), טבלת `user_stores`. | 🔒 **הצמצום הוא הנעילה ההרמטית — מקום אחד, כל הדפים** (ראו "נעילה הרמטית" למטה): כל `scopeWhere`/`scopedStoreList`/`assertInScope`/`assertStoreAllowed` יורש אותו, כולל דפים עתידיים. `res.locals.availableStores` נשאר **לא-מצומצם** אחרת אי אפשר להחליף חנות. הבורר בבאנר (`header.ejs`) חייב את `/context` ב-`OPEN_PATHS` אחרת תפקיד מוגבל חסום. `authorizedStoreIds`: אין grants → כל חנויות החברות המורשות (תאימות לאחור). טפסי יצירה (חשבונית/סגירה) ננעלים ל-`activeStore`. |

---

## 🧭 Chrome גלובלי (מופיע בכל דף)

**קובץ:** `partials/header.ejs` (nav + theme), `partials/footer.ejs` (enhancers + מחוות מובייל), `partials/_ownerDialogs.ejs` (חלונות בעלים).

| רכיב | מה עושה / מחווט | אם מוחקים/משנים/מזיזים |
|---|---|---|
| **תפריט ניווט (☰ / סרגל צד)** | לינקים לפי `nav('nav_*')` = `canView` (`lib/permissions.js`). כל לינק מוגן גם ב-mount (`app.js`) ב-`requirePageAccess`. | מחיקת לינק = הדף עדיין נגיש ב-URL ישיר לבעלים. הוספת לינק בלי `requirePageAccess` במאונט = פרצת גישה. |
| **מתג מצב בהיר/כהה** (🌙/☀️) | `apToggleTheme()` שם `data-theme=light` על `<html>` + שומר `ap-theme` ב-localStorage. סקריפט ב-`<head>` מיישם לפני צביעה. פלטה: `nocturne.css` תחת `:root[data-theme="light"]`. | הסרת סקריפט ה-`<head>` → הבזק כהה→בהיר בטעינה. שינוי שם המפתח `ap-theme` → מאבד העדפות שמורות. |
| **לינק "צילום חשבוניות"** | מסומן 🔒 "נעול" כש-`scanEnabled=false` (מגיע מ-`res.locals.scanEnabled` ב-`app.js`). | ראה "נעילת צילום" למטה — התלוי ב-`app_settings.scan_enabled`. |
| **בורר תאריך (footer)** | מחליף `input[type=date]` → תצוגה DD/MM/YY, הקלדה במחשב (`parseTyped`). שומר hidden ISO בשם המקורי. | שינוי → משפיע על **כל** שדות התאריך. `data-dp-mode=week/month` נשארים picker-only. |
| **קומבובוקס (footer)** | מחליף `<select class="js-combo">` בשדה חיפוש; ה-select נשאר הערך הנשלח. | הסרת המחלקה `js-combo` מהשדה מחזירה select רגיל (לא שובר). שינוי הלוגיקה משפיע על שדה הספק בחשבוניות. |
| **חלונות בעלים** (`_ownerDialogs.ejs`) | כפתור `apOpen('dlg-X')` + `<dialog id="dlg-X">` **חייבים לנסוע יחד** (מבחן `orgs-permissions.test.js` אוכף). | כפתור בלי הדיאלוג = כפתור מת (showModal על null). מחיקת דיאלוג → הסר גם את הכפתור + עדכן את מערך `ACTIONS` במבחן. |
| **באנר "חנות פעילה"** (`header.ejs`, ראשון ב-`<main>`) | מציג `activeStore` (או "כל החנויות"); בורר `<select onchange=submit>` + כפתור מחווט ל-`POST /context/store` (cookie `ap_store`). CSS `.store-banner` ב-`nocturne.css`. נעילה אוטומטית כשיש חנות זמינה אחת. | מוצג בכל דף כשיש `availableStores`. הסרת ה-cookie/route → אין הקשר. תלוי ב-`res.locals.availableStores/activeStore` מ-`currentUser`. |

---

## 🏠 לוח בקרה — `routes/index.js` (`/`), `views/dashboard.ejs`

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| חיפוש (`q`) + חנות/חברה | `GET /` | `invoiceLookup` (`reports.js`), `lookupChecks` (`payments.js`), `searchSuppliers` | **רב-מונחים**: `lib/search.js` (`parseSearchTerms`/`anyTermLike`) — פיצול רווח/פסיק/;/שורה, OR. תוצאות: חשבוניות/צ׳קים/ספקים. Enter שולח (טופס רגיל). | שינוי `lib/search.js` משפיע גם על רשימת החשבוניות. הסרת `q` = אין חיפוש בלוח. |
| "רק שלא שולמו" | `GET /?unpaid=1` | `invoiceLookup({unpaidOnly})` → `AND i.status<>'paid'` | מסנן תוצאות החשבוניות לפתוחות. | תלוי בסטטוסי החשבונית — ראה "סטטוס חשבונית". |
| קוביות (ספקים ממתינים, מוחזקות, צ׳קים בחוץ, מזומן לא-משויך, הפקדות, פערי Z) | `GET /` | `dashboardStats(scope, storeId)`, `outstandingChecks(scope, {storeId})`, `unmatchedCashExpenses(scope, limit, storeId)`, `listDeposits({storeId})`, `zSequenceStatus(scope, storeId)` | קריאה-בלבד; לינקים לדפי המקור. **כל מקורות הנתונים בלוח מכובדים להקשר החנות הפעילה** (`storeId`=activeStoreId): stats, "צ׳קים בחוץ" (oc_store), "תשלום במזומן ללא התאמה" (שני צדדי ה-UNION מסוננים לפי `z.store_id`/`zc.store_id`; **הוצאת מזומן שיש לה תשלום מזומן תואם — אותו store_id+amount, non-voided — מנוכה 1:1 ולא מופיעה**, "תשלום מזומן = מטופל"), "היסטוריית הפקדות", ופערי-Z, וגם **"התאמת בנק אחרונה"** (`lastReconciliationFor(scope,storeId)`): תאריך לפי החנות הפעילה; ב"כל החנויות" מציג את התאריך האחרון **+ שם החנות** שלו. נגזר ישירות מ**תנועות בנק מותאמות** (`bank_transactions.matched_payment_id IS NOT NULL` → `bank_account_id` → store) — שיוך-חנות מדויק בלי תלות ב-audit_log; חנות בלי תנועות מותאמות מציגה "—" (לעולם לא תאריך של חנות אחרת). סקופ+בחירה ב-JS ל-pg-mem. תג "N לטיפול" בכותרת המזומן. | **כשמוסיפים מקור נתונים לדשבורד — לחווט אליו `storeId`.** `dashboardStats`/`zSequenceStatus`/`unmatchedCashExpenses` קיבלו פרמטר מיקום `storeId` (לפני `x`) — קוראים חייבים לעדכן. `outstandingChecks` storeId מסונן ב-JS. |
| אישורים (`/approvals`) | `GET /approvals`, `POST /approvals/:id/approve|reject` | `services/changeRequests.js` + `listSuppliers('pending')` | שני חלקים: **ספקים ממתינים לאישור** (רשימה עם "אשר ספק"/"חסום" → `POST /suppliers/:id/{approve,block}` עם `return_to=/approvals`), ועריכות של לא-בעלים (change_requests) → מיושמות ב-approve. תג הנאב "אישורים" = `countPending()`+`countPendingSuppliers()` (`currentUser`). | מחיקה = לא-בעלים לא יכולים לבקש שינויים / הבעלים לא רואה ספק ממתין באישורים. `safeReturn` בראוטי הספק מגביל ל-redirect יחסי. |
| יומן (`/audit`) | `GET /audit`, `POST /audit/events…` | `services/calendar.js`, `services/audit.js` | לוח שנה + לוג פעולות + תזכורות פוש. | קריאה-בלבד ברובו. |
| **התראות (🔔 `/notifications`)** | `GET /notifications`, `POST /notifications/{:id/read,read-all}` (`requireOwner`) | `services/notifications.js` (טבלת **`notifications`**, סכימה ×3+migrate) | ערוץ התראות בתוך האפליקציה — **`notify()` (`lib/notify.js`) רושם כאן כל התראה בנוסף לטלגרם** (HTML→טקסט, שורה ראשונה=כותרת), כך שהבעלים מכוסה גם בלי טלגרם. פעמון בכותרת עם מונה שלא-נקרא (`res.locals.unreadNotifications` ב-`currentUser`, בעלים בלבד, tolerant). קריאת-סמן גלובלית (`read_at`). | הסרת הטבלה/השירות שוברת את `notify` (עטוף ב-try/catch — יהפוך ל-no-op). מונה הפעמון tolerant לפני "עדכן מסד נתונים". |

---

## 🧾 חשבוניות — `routes/invoices.js`, `services/invoices.js`, `views/invoices/*`

**מוגן ב-`requirePageAccess('nav_invoices')`.**

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים/מזיזים |
|---|---|---|---|---|
| רשימה + סינון (סטטוס/ספק/חנות/תאריך/`q`) + עמודת "שולם" | `GET /invoices` | `listInvoices` → `enrichPaidStatus` (JS, לא JOIN — pg-mem לא תומך ב-join על derived) | טאבים כולל **`unpaid`** וירטואלי. `q` רב-מונחי. עמודת "שולם": אמצעי+מזהה מ-`payment_lines`, "שולם במזומן" אם יש התאמת `z_closing_expenses/z_expenses`, אחרת "לא שולם". **תשלום מזומן = מטופל** (החלטת בעלים): חשבונית עם תשלום מזומן רשום **לא** מסומנת "ללא התאמה" ואין שורה אדומה (הדגל הוסר; נשאר רק `row-hold` ל-on_hold). ברירת-מחדל storeId = חנות פעילה. **סדר הרשימה: `groupByPayment`** — חשבוניות ששולמו יחד (אותו `pay_id`) מקובצות רצוף אחת מתחת לשניה, בסדר השמירה (id עולה בתוך הקבוצה), והקבוצה מעוגנת ל-id הגבוה שלה כך שהרשימה נשארת חדש-ראשון; שינוי-סדר טהור (אין הוספה/הסרה). | שינוי מיפוי הסטטוס → משפיע על הטאבים. `enrichPaidStatus` שדות `pay_id`/`pay_*`/`cash_matches` נצרכים ב-`views/invoices/index.ejs` ו-`pay_id` ב-`groupByPayment`. |
| חשבונית חדשה + תשלום מוטמע | `GET /invoices/new`, `POST /invoices` | `createInvoice` (+יצירת תשלום) | R2 (כפילות הקצאה=חסימה קשה), R3 (מס מעל סף בלי הקצאה→on_hold). שדה ספק=קומבובוקס. שדה "סכום כולל מע"מ" מסגרת צהובה (`input-hl-yellow`). מספר הקצאה **≥6 ספרות** (`normalizeAllocation` `/^\d{6,}$/`). **"שמור והוסף עוד לספק"** (`action=add_another`) → redirect עם `&added=1&doc=credit_note`, וה-GET מגדיר את סוג המסמך הבא ל**זיכוי** כברירת מחדל. **עם `added=1` (ואין `pick` מפורש) ה-GET מסמן-מראש את כל `batchInvoices` (כל החשבוניות הפתוחות של אותו ספק+חנות)** → קטע "שיוך לתשלום" למטה מסכם אוטומטית את הנטו שלהן (R5, זיכויים מקזזים) לתשלום אחד; המשתמש יכול לבטל סימון. | שינוי `normalizeAllocation` = משנה ולידציה בכל 4 שדות ההקצאה (new/edit/show/OCR-apply). `batchContext`=`listPayable` מסונן לספק+חנות. הסרת השדה הצהוב = ויזואלי. |
| **תשלום מרוכז → חזרה לדף החשבוניות** | `POST /invoices/pay-batch` → redirect `/invoices?paid=<id>&nsup=&nstore=` | `createPayment` → באנר ב-`views/invoices/index.ejs` (`paidNotice`) | אחרי יצירת התשלום המרוכז **נשארים בדף החשבוניות** (לא עוברים למרקורים) כדי להמשיך להזין; הבאנר נותן "צפה בתשלום" + "➕ חשבונית נוספת לאותו ספק". `nsup`/`nstore` נושאים ספק+חנות לקיצור-הדרך ואינם מסננים את הרשימה. | שינוי ה-redirect חזרה ל-`/payments/:id` מחזיר את הקטיעה בזרימת ההזנה. |
| "צרף חשבונית פתוחה" (📎) | `GET /invoices/new?…&pick=ids` | `listPayable` → dialog | בוחר חשבוניות פתוחות של **ספק אחד** → ניווט עם `pick` → מסמן מראש בקטע התשלום המרוכז. הדיאלוג מציג **נטו לתשלום חי** (`.oi-check[data-amount]`, זיכויים מקזזים). | תלוי ב-`listPayable` (status recorded/approved) ובקטע pay-batch. |
| תשלום מרוכז | `POST /invoices/pay-batch` | `createPayment` (רב-חשבוניות, זיכוי=שלילי, net=Σ) | `invoice_ids[]` (מפתחות חוזרים!) + אמצעי תשלום → תשלום אחד. נקודת כניסה נוספת: **בחירה מרובה ברשימת החשבוניות** (checkbox `.inv-pick[data-amount]`, אכיפת ספק+חנות אחת ב-JS) → `#payBar` מציג ספירה + **נטו לתשלום חי** (`#payNet`) → "הנפק תשלום" מנווט ל-`/invoices/new?supplier=&store=&pick=ids`. הגעה עם `?pick=` **גוללת אוטומטית** ל-`#batchPay` וממקדת את אמצעי התשלום (בלי לגלול מעבר לטופס חשבונית-חדשה). | תלוי ב-R1/R5. שינוי encoding (מערך→מחרוזת) שובר. ה-checkbox מסומן רק לחשבוניות payable שלא שולמו. |
| התאמת הוצאת-מזומן↔חשבונית | `POST /invoices/{match-cash,unmatch-cash}` | `matchClosingExpenseToInvoice`/`unmatchClosingExpense` (`services/zclosing.js`) | ברובריקת "הוצאות מזומן (מסגירות Z)" בדף חשבוניות (`_cashExpenses.ejs` במצב `matchable`): בורר חשבונית + "התאם" → מציב `z_closing_expenses.invoice_id` → החשבונית קוראת "שולם במזומן". סקופ נבדק בשני הצדדים **לפני** השינוי. | הרובריקה משותפת עם דף סגירת-Z (שם `matchable` כבוי). `matchInvoices`=`listPayable` בסקופ. |
| **R3 — מספר הקצאה** | חישוב יחיד ב-`services/invoices.js#requiresAllocationNumber` | נקרא מ-`createInvoice`, `updateInvoice`, `approveInvoiceForPayment`, `reconcileR3Hold` **ו-`views/invoices/show.ejs`** (דרך `res.locals.requiresAllocationNumber`) | **🔴 הבדיקה היא לפי סכום המע"מ, לא לפי הסכום לפני מע"מ.** החוק נוקב בסכום נטו (5,000 ₪ מ-1.6.2026) אבל השו"ת של רשות המסים קובע שהקריטריון הוא **המע"מ הנגזר ממנו** (900 ₪), ובחשבונית מעורבת הוא נשאר המע"מ "ללא קשר לכך שהיא כוללת עסקאות פטורות או בשיעור אפס". ל-18% רגיל שתי הבדיקות זהות (5,000×18%=900) — ההבדל הוא ב**מס בשיעור אפס**: **פירות וירקות טריים שלא עברו עיבוד הם 0% לפי §30(א)(13) לחוק מע"מ**, ולכן חשבונית של ירקן נושאת מע"מ 0 ו**אינה חייבת מספר הקצאה בשום סכום**. בדיקה לפי הנטו החזיקה (on_hold) כל חשבונית כזו בשביל מספר שלא קיים. `config.rules.allocationVatThresholdAgorot` נגזר מ-`allocationThresholdAgorot × vatRate`. | ריכוז הכלל למקום אחד הוא מה שמונע סחיפה — היו ארבעה עותקים של הביטוי, אחד מהם ב-EJS. אל תשכפל. שינוי חזרה לבדיקה לפי הנטו מחזיר את החסימה השגויה על ספקי פירות וירקות. `test/allocation-zero-rated.test.js`. |
| **תזכורת "חשבונית מס ללא מע"מ"** | `zeroVatNeedsCheck` (display-time, `views/invoices/show.ejs`) | `getInvoiceDetail` מביא `s.zero_rated AS supplier_zero_rated`; `res.locals.zeroVatNeedsCheck` | הרשת שנפרמה כש-R3 עבר למע"מ: חשבונית מס מעל הסף נטו עם מע"מ ≈0 היא או עסקה בשיעור אפס (תקין) או **מע"מ שנשכח** (ואז כן נדרש מספר הקצאה). מסמנים את הספק כ"עסקאות בשיעור אפס" (`suppliers.zero_rated`, סכימה ×3+migrate) והתזכורת נעלמת לו. **בכוונה לא אזהרת-שמירה** — זו בדיקת שפיות בזמן עיון, לא סיכון כפילות. | הדגל **לא** עוקף את R3 — R3 תמיד לפי המע"מ שעל החשבונית בפועל, כך שחשבונית מעורבת של אותו ספק עדיין נחסמת. תיבת הסימון ב-`suppliers/new.ejs`+`edit.ejs`, תג "מע"מ 0%" ב-`suppliers/index.ejs`. |
| **שיוך ספקים לחנויות לפי היסטוריית החשבוניות** | רובריקה ב-`GET /suppliers` + `POST /suppliers/assign-stores` (**`requireOwner`**) | `services/suppliers.js#supplierStoreSuggestions` + `setSupplierStores` | **התשובה ל"למה אני רואה ספקים של חנויות אחרות":** ספק **ללא** שורה ב-`supplier_stores` משותף לכל החנויות ולעולם לא מוסתר (`filterByStoreLinks` — מכוון, כדי שכלום לא ייעלם עם הוספת השיוך), ולכן עד שמשייכים, בחירת סניף לא מצמצמת את רשימת הספקים. הרובריקה מציעה שיוך **מהחשבוניות שהספק הגיש בפועל** (`GROUP BY supplier_id, store_id`), עם מספר החשבוניות לכל חנות; ספק שהגיש לשני סניפים מקבל את שניהם. מוצעים רק ספקים **ללא** שיוך — שיוך קיים הוא החלטה של מישהו. בטבלה עצמה "—" הוחלף בתג **"כל החנויות"**, כי זה לא נתון חסר אלא מצב. | עובד דרך **`assignmentScope(req)`** (ההרשאות הלא-מצומצמות) כמו כל בורר "העתק לחנות" — אחרת נעילת החנות הפעילה הייתה מונעת מהבעלים לשייך ספק לסניף שהוא באמת משרת; כל `store_id` נשלח עדיין דרך `assertStoreIdsAllowed`. חנות מחוץ להרשאות **לא מוזכרת בחזרה** ואף לא נספרת. `test/supplier-store-assign.test.js`. |
| **סימון ספקי פירות וירקות (בכמות)** | רובריקה ב-`GET /suppliers` + `POST /suppliers/zero-rated` (**`requireOwner`**); גם שתי אפשרויות בבורר הפעולות המרוכזות (`POST /suppliers/bulk`) | `services/suppliers.js#zeroRatedCandidates` + `setSuppliersZeroRated` | מזהה מועמדים **מהחשבוניות של הספק עצמו** — ספק שכל חשבוניות המס שלו נושאות מע"מ 0 הוא בדיוק איך שירקן נראה בנתונים, וזה הדבר היחיד שמסמן מראש (`suggested`). **שם** ("פירות"/"ירקן"/"חקלא"…) מוצג כרמז ומעלה את הספק לרשימה, אבל **לעולם לא מסמן לבד**. הטופס מקבל את הסימון כתשובה שלמה: מה שסומן יסומן, ומה שהוצע ולא סומן — הסימון יוסר ממנו. | שתי שאילתות פשוטות + join ב-JS (pg-mem לא תומך ב-GROUP BY מעל join). הרובריקה מוצגת לבעלים בלבד. `zeroCandidates` חייב להיות ב-locals של **שני** מסלולי ה-render של `/suppliers`. |
| דף חשבונית + מספר חשבונית | `GET /invoices/:id` | `getInvoiceDetail` (`SELECT i.*`) | מציג מספר חשבונית + שורות (אם מסריקה) + תמונה/OCR. שורת התשלום משתמשת ב-`paymentIdent(invoice.payment)` — לכן `row.payment` **חייב** לכלול את `p.method` + שדות המזהה (reference/batch_number/card_last4/payer_name/check_number), אחרת התווית נופלת ל"צ׳ק" גם על מזומן/העברה. | הסתרת מספר החשבונית הייתה בעבר — הוחזרה. הסרת `p.method` מה-SELECT מחזירה את הבאג "שולם בצ׳ק" על כל אמצעי תשלום. |
| תפריט **אפשרויות** (גם אחרי תשלום) | לינקים | `can('edit_invoice')` / `can('approve_payment')` | ✏️ ערוך חשבונית (מוסתר כש-`status==='paid'`) · 💳 ערוך אמצעי תשלום (אם יש תשלום) · ➕ לאותו ספק (`?supplier=&store=`). | התפריט מוצג גם לחשבונית ששולמה (בשביל תשלום/הוספה). עריכת **נתוני** חשבונית ששולמה חסומה — `GET /:id/edit` מפנה (303) חזרה; מתקנים דרך "ערוך אמצעי תשלום". |
| ערוך חשבונית | `GET/POST /invoices/:id/edit` | `updateInvoice` (`requirePermission('edit_invoice')`) | עריכת ספק/מספר/סכום/הקצאה; חוסם חשבונית ששולמה. **R3 נבדק מחדש סימטרית:** עריכה שמורידה מתחת לסף / מוסיפה הקצאה → מנקה החזקת-R3 ישנה; עריכה שמעלה חשבונית `recorded` מעל הסף בלי הקצאה → **מציבה** החזקת-R3 (אחרת עריכה-כלפי-מעלה עוקפת את R3 בשקט). החזקות ידניות (reason שלא מתחיל ב-"R3") ומצבים מכוונים (approved/paid) לא נוגעים. | R2 נבדק מחדש. הצבת ההחזקה חלה רק על סטטוס `recorded`. |
| תשלום/אישור (תפריט) | `POST /invoices/:id/{approve,request-payment,release,hold,allocation}` | `services/invoices.js` | R3 (הקצאה), R1/R5 (תשלום), החזקה (on_hold) + סיבה + תזכורת פוש. | שינוי סטטוסים משפיע על התשלום ועל ה-firewall. |
| תמונה/OCR | `POST /invoices/:id/{image,ocr,ocr/apply,ocr/delete,image/delete}` | `services/ocr.js`, `lib/storage.js` | העלאת צילום (שחור-לבן), OCR כתמיכת החלטה בלבד (לא דורס ערכים). | `image_path`/`invoice_ocr` (PK=`invoice_id`, ON DELETE CASCADE). |

**סטטוס חשבונית:** `recorded → approved_for_payment → paid`, או `on_hold`. `unpaid` = כל מה ש-`<>'paid'`. משפיע על: firewall, pay-batch, "לא שולמו", `listPayable`.

---

## 💳 תשלומים/צ׳קים — `routes/payments.js`, `services/payments.js`, `views/payments/*`

**מוגן ב-`requirePageAccess('nav_payments')`.**

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים/מזיזים |
|---|---|---|---|---|
| רשימה + סינון-עמודות | `GET /payments` | `listPayments` | סינון client-side בשורת ה-`.filter-row`. | ויזואלי. |
| תשלום חדש | `GET /payments/new`, `POST /payments` | `createPayment` | R1 (ספק+חשבונית מאושרים), R5 (סכום=Σ שורות), מזהה לפי אמצעי (צ׳ק=מספר צ׳ק וכו'), תקרת מזומן. **בודק מראש כפילות מספר צ׳ק פעיל** → הודעה בעברית. **"שם המשלם" (מזומן) הוא בורר עובדים (`select.js-combo`), לא טקסט חופשי** — ראו "שדות שם" למטה; ה-route מזרים `employeeOptions` (שני מסלולי ה-render: GET ו-re-render של שגיאה). | R1/R5 קריטיים. שינוי המזהים משפיע על `getCheckPrintData`. **`onMethod` משבית `input, select` בשורות מוסתרות** — אם מצמצמים חזרה ל-`input`, בורר המזומן ישלח ערך גם בתשלום צ׳ק. |
| דף תשלום + סטטוס | `GET /payments/:id` | `getPaymentDetail` | פירעון/ביטול דרך תגית הסטטוס. | — |
| **ערוך אמצעי תשלום (בעלים בלבד)** | `GET/POST /payments/:id/edit` | `updatePayment` (**`requireOwner`**) | **בעלים בלבד** — כלל עסקי: לא עורכים אמצעי תשלום שהוזן; משנים ע"י ביטול+הנפקה מחדש. עריכה נשארה לבעלים כפתח-מילוט לטעות אנושית (בעיקר זיכוי שהוזן אחרי שסכום הצ׳ק כבר נרשם → re-target מחשב net מחדש). הכפתור ב-`invoices/show.ejs` מוצג רק ל-`isOwner` (`res.locals.isOwner` ב-`currentUser`). עריכת אמצעי/מזהה/תאריך. **ורק לצ׳ק "הונפק" ולא מותאם-בנק** — re-target של החשבוניות: הוספת זיכוי → הסכום=net מחדש (R5 נשמר, לא נערך ידנית). **תקרת מזומן נאכפת בשני הענפים** — גם בעריכה רגילה (בלי re-target) ששינתה את האמצעי ל-`cash`, הסכום הקיים נבדק מול `cashCeilingAgorot` (אחרת צ׳ק גדול הוסב ל"מזומן" מעל הסף). | מסך `payments/edit.ejs`. **"שולם ע"י" הוא אותו בורר עובדים** (`employeeOptions` מוזרם משני מסלולי ה-render). re-target משנה סטטוסי חשבוניות (paid↔recorded). חסום ל-cleared/voided/matched. |
| נפרע / החזר / בטל | `POST /payments/:id/{clear,unclear,void}` | `markCleared`,`markIssued`,`voidPayment` | ביטול → **משחרר את מספר הצ׳ק** (האינדקס `ux_payments_account_check` מחריג `status='voided'`). **כלל ביטול: מותר רק כל עוד `status='issued'` ולא הותאם לתנועת בנק** — תשלום שנפרע/עבר בבנק אינו הפיך (העברה/מזומן שעברו — לא מבטלים; צ׳ק — ביטול+הנפקה מחדש). סדר הבדיקות ב-`voidPayment`: כבר-מבוטל → מותאם-בנק → נפרע. לביטול תשלום שנפרע בטעות: `markIssued` (שחרור פירעון) + ביטול התאמה, ואז void. **`voidPayment` חוסם ביטול-כפול** (`status==='voided'`). **`markCleared` קובע `cleared_date` ברירת-מחדל לפי `israelToday()`** (Asia/Jerusalem, לא UTC — קרוב לחצות ה-UTC היה יום לפני/אחרי). טפסי `/void` (וכל טופס עם `data-busy`) מקבלים **משוב מיידי** בשליחה (כפתור→disabled + "מבטל…", enhancer גלובלי ב-`footer.ejs`). | שינוי האינדקס (הסרת החרגת voided) מחזיר את הבאג "אי אפשר להנפיק שוב מספר צ׳ק שבוטל". `israelToday()` ב-`lib/loginHours.js`. |
| הדפסת צ׳ק | `GET /payments/:id/print` | `getCheckPrintData`, `views/payments/print.ejs` | פריסת Standard-501; פעיל תחת `config.checkPrinting.approved`. | דורש MICR font + אישור בנק. |
| התאמה אוטומטית | `POST /payments/auto-reconcile` | `services/reconciliation.js` | — | ראה התאמת בנק. |

- **התראת "תשלום מוקדם מתנאי התשלום"** (`earlyPaymentAlerts` ב-`services/payments.js`, נשלחת אחרי commit ב-`createPayment` דרך `notify`) — **שעון ימי האשראי מתחיל בחשבונית המס בלבד**: שורה עם `docType==='credit_note'` מדולגת (זיכוי מקטין סכום, לא קובע תאריך יעד), וכששולמו כמה חשבוניות מס של אותו ספק בתשלום אחד **התאריך הקובע הוא של חשבונית המס המוקדמת ביותר** (חלון התנאים שלה נסגר ראשון) — לכן **התראה אחת לכל ספק**, לא אחת לכל חשבונית. בתשלום מרוכז למשפחת ספקים (טרה+קוקה קולה) כל ספק שומר על השעון שלו. שורה בלי `docType` נחשבת חשבונית מס (תאימות לאחור). `termsRows` ב-`createPayment` דוחף `supplierId`+`docType` — הסרתם מחזירה את הבאג (התראה לפי תאריך הזיכוי).

### R8 — שיוך תשלומים לחשבוניות (חשבונית שהגיעה *אחרי* שהכסף שולם)
עד כה הקשר היה חד-כיווני: תשלום "בלע" כל חשבונית **במלואה**, וחשבונית שייכה לתשלום אחד. זה כיסה
"כמה חשבוניות + זיכויים על צ׳ק אחד" (עבד תמיד) אבל לא את המראה: **12 צ׳קים לשכירות שנמסרו מראש,
ואז חשבונית אחת עבור 3 חודשים.** הטבלה `payment_lines` כבר הייתה רבים-לרבים — מה שחסר היו הכללים.

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| **תשלום על החשבון** (ללא חשבונית) | `POST /payments` עם `advance_supplier_id`+`advance_amount` (רובריקה 💰 ב-`payments/new.ejs`) | `createPayment` (ענף `isAdvance`) | תשלום בלי שורות: חובה **ספק + סכום חיובי**, והסכום כולו יושב כ"יתרה על החשבון". **`payments.supplier_id`** (סכימה ×3 + `migratePaymentSupplier`) קיים בדיוק בשביל זה — בלי שורות אין דרך אחרת לדעת למי שולם. סכום ידני **נדחה** כשמצורפות חשבוניות (שם הוא נגזר — R5). | הסרת `supplier_id` = מקדמות מתייתמות ולא יימצאו לשיוך. |
| **שייך לתשלומים שכבר בוצעו** | `POST /invoices/:id/allocate` (`requirePermission('approve_payment')` + `assertInScope('invoice')`) | `allocateInvoiceToPayments` (`services/allocations.js`), רובריקה 🔗 ב-`invoices/show.ejs` | מציע את **המקדמות הפתוחות של אותו ספק באותו חשבון בנק** (`openAdvancesForSupplier`, לפי תאריך) ומשייך עד גובה היתרה. כל שיוך **מוגבל בשני הצדדים** — לא יותר מהיתרה בתשלום ולא יותר מהיתרה בחשבונית. | בלי החסימה הכפולה אפשר "לשלם" פעמיים על אותו כסף. |
| **שייך חשבוניות לתשלום** (הכיוון ההפוך) | `POST /payments/:id/allocate` | `openInvoicesForPayment` → `allocateInvoiceToPayments`, רובריקה 💰 ב-`payments/show.ejs` | מוצג רק כשלתשלום יש **יתרה על החשבון** והוא `issued`. מציע את החשבוניות הפתוחות של **משפחת הספק של אותו תשלום** באותה חנות (חשבונית זיכוי לא נכללת), ומשייך אותן בזו אחר זו. | הרובריקה נעלמת כשהיתרה 0 — זה הסימן שהתשלום מנוצל. |
| **שמור וצור תשלום נוסף לחשבונית** (פיצול צ׳קים) | `POST /invoices/pay-batch` עם `pay_amount` ו/או `add_more=1` (רובריקת "שיוך לתשלום" ב-`invoices/new.ejs`) | `createPayment` (מקדמה) → `allocateInvoiceToPayments` בלולאה | חשבונית שמשולמת בכמה צ׳קים: מזינים סכום צ׳ק ולוחצים — התשלום נקלט, **היתרה מתעדכנת**, אותן חשבוניות נשארות מסומנות (`?pick=`+`?partial=`), ומזינים את הצ׳ק הבא. הכפתור הרגיל ללא סכום סוגר את **כל היתרה שנותרה** (לא את ערך החשבונית). | **סכום מעל היתרה נדחה, לא נחתך** — "לא ניתן לשלם מעבר ליתרה…", והטופס חוזר עם אותה בחירה ואותם פרטים לתיקון הסכום בשדה אחד (`pickIds`+`payValues`). לא נרשם כלום. חשבוניות סגורות → שגיאה, לא תשלום כפול. |
| **בטל שיוך** | `POST /invoices/:id/deallocate` | `deallocate` | הכסף חוזר ליתרה על החשבון והחשבונית שבה להיות ברת-תשלום. חסום כשהתשלום כבר **נפרע**. | — |

- **שני גדלים נגזרים נושאים את כל הפיצ'ר:** `payment.unallocated = amount − Σ שורותיו` (יתרה על החשבון)
  ו-`invoice.open = total_amount − Σ שורותיה` (יתרה לתשלום). תשלום ישן ומשויך במלואו קורא בדיוק כמו קודם (`unallocated=0`) — **אין רגרסיה**.
- **`syncInvoicePaidStatus` הוא המקום היחיד שמחליט "שולם"** — נקרא אחרי כל שיוך, ביטול-שיוך **וביטול תשלום**. סטטוס החשבונית נשאר ה-enum הקיים (בלי ערך חדש, בלי בניית CHECK מחדש): מכוסה במלואה ⇒ `paid`, אחרת חוזר ל-`approved_for_payment`. **`on_hold` (R3) גובר** — שיוך לא מסיר החזקה.
- **🔴 `voidPayment` מבטל קודם ואז מחשב מחדש** — חשבונית יכולה להיות מכוסה בכמה תשלומים, כך שהחזרה עיוורת ל-`approved_for_payment` הייתה פותחת מחדש חשבונית שהצ׳קים האחרים עדיין מכסים.
- **זיכוי לא משויך לתשלום קיים** (סכום שלילי) — הוא מתקזז על תשלום חדש יחד עם חשבונית המס, כמו תמיד.
- `getInvoiceDetail` מחזיר כעת **`payments`** (מערך, ללא מבוטלים, עם `amount_applied`) + `payment` = הראשון לתאימות, ו-`derived_bank_account_id`.
- **מתי `pay-batch` מפצל:** כשהוזן `pay_amount`, **או** כששאר היתרה כבר קטנה מערך החשבוניות (כלומר צ׳ק קודם כבר כיסה חלק). אחרת נשמר המסלול הישן (`createPayment` עם `invoiceIds`) על כל בדיקותיו. **זיכוי בפיצול:** `allocateInvoiceToPayments` מטפל גם בזיכוי — השורה שלילית, ולכן במקום לצרוך יתרה בתשלום היא **מגדילה את מה שהצ׳ק יכול לבלוע** (צ׳ק ₪5,000 עם זיכוי ₪1,000− מכסה ₪6,000 חשבונית). זיכוי הוא מסמך אחד: הוא נכנס **בשלמותו לתשלום אחד** ולא מתפצל. 🔴 **הסדר קריטי** — ה-route ממיין **זיכויים ראשונים** (`ordered.sort`); הפוך, הצ׳ק היה מתמלא מהחשבונית והזיכוי היה נדחק לתשלום מאוחר יותר וקיזוז שגוי. "עדיין פתוח" מתהפך בסימן (`stillOpen()`). `batchContext` מחזיר `open_amount` לכל שורה והטופס מציג "נותרו …" + "יתרה פתוחה במסומנות".
- **הרובריקה 🔗 בדף החשבונית מוצגת תמיד כשיש יתרה פתוחה** (`canAllocate`), גם כשאין מקדמות — עם הסבר וקישור ל"תשלום על החשבון". קודם היא נעלמה לגמרי ואי אפשר היה למצוא את הפיצ'ר.
- **🔴 תשלום אחד = ספק אחד** (`createPayment` **ו-`updatePayment`/re-target): כל החשבוניות בתשלום חייבות להיות מאותה **משפחת ספקים** (`supplierFamilyIds` — ספק ובנותיו, למשל טרה תחת קוקה קולה). ניסיון לשלם לשני ספקים לא-קשורים נדחה (R1) והתשלום כולו מתגלגל אחורה. ב-UI (`payments/new.ejs`) `lockFamily()` נועל את כל המשפחות האחרות ברגע שסומנה חשבונית — הצ׳ק-בוקסים מקבלים `data-fam` = `supplier_parent_id || supplier_id`, ושדה "ספק" לקריאה בלבד מראה למי משלמים. **החסימה בשרת היא הקובעת** — ה-JS הוא נוחות בלבד.
- **🔴 שדות שם = בורר מרשימת העובדים, אף פעם לא הקלדה חופשית.** כלל אחד לכל מקום שבו נרשם *מי*:
  "עובד (מבצע הספירה)" ו-"שם" בהוצאות מזומן (`views/zclosing/index.ejs`, `views/zclosing/edit.ejs`,
  `views/reports/_zform.ejs`) ו-"שם המשלם"/"שולם ע"י" בתשלום מזומן (`views/payments/new.ejs`,
  `views/payments/edit.ejs`). שם מוקלד לא הותאם לאף עובד — אותו אדם הופיע בשלושה איותים והשורה
  הייתה בלתי-ניתנת לייחוס. בכל מקום: הערך הנשלח נשאר מחרוזת (חוזה השרת לא השתנה), שם שנשמר לפני
  השינוי (או עובד שהוסר) נשאר בר-בחירה ומסומן "(לא ברשימת העובדים)", ובלי עובדים כלל יש נפילה חזרה
  ל-input חופשי כדי שהטופס לא ייתקע. הכלל נעול ב-`test/staff-name-fields.test.js`.
- **דף תשלום חדש מכבד את החנות הפעילה:** `GET /payments/new` מסנן `listPayable` ל-`req.activeStoreId`; בלי חנות פעילה מוצגות כל החנויות אבל **הקבוצות מקופלות כברירת מחדל** (`<details data-accordion="paygrp">`, `open` רק כשיש חנות פעילה או קבוצה יחידה). ברובריקת "תשלום על החשבון" יש **"שמור והוסף עוד תשלום"** (`add_another=1` → redirect ל-`/payments/new?added=1`) — להזנת 12 צ׳קי שכירות ברצף.
- **גוצ'ה pg-mem:** `openAdvancesForSupplier` מפוצל לכמה שאילתות פשוטות בכוונה — pg-mem לא תומך ב-EXISTS מתואם ולא בכלל "GROUP BY על מפתח ראשי + SELECT כל העמודות". אל תאחד אותן חזרה לשאילתה אחת "חכמה".

**קריטי:** האינדקס `ux_payments_account_check` = `(bank_account_id, check_number) WHERE check_number IS NOT NULL AND status<>'voided'` — קיים ב-schema.sql / schema.pg.sql / migrate.js (`migrateCheckNumberReuse`). שינוי בו = שנה בכל שלושתם.

---

## 📊 דוחות Z — `routes/reports.js`, `services/zreports.js`, `views/reports/*`

**`/reports` — הגישה מוגנת per-sub-page בתוך ה-router.**

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| דוחות Z (רשימה/הוספה/עריכה) | `GET/POST /reports/zreports[/:id]` | `_zform.ejs` (טופס משותף) | הוצאות מזומן עם **kind** (manual/salary/advance/invoice→`description_type`) המקשר עובד/חשבונית. | `z_expenses.invoice_id`/`employee_id` (nullable). מחיקת Z → cascade ל-`z_expenses`. |
| **איזון קופות בעריכת סגירה** | `GET /zclosing/:id` | `views/zclosing/edit.ejs` → `closing.registers` (JSON) | הרובריקה **נפתחת כשיש בתוכה ספירה**, והכותרת אומרת "N קופות · ₪X". הספירה תמיד נשמרה ותמיד נטענה — אבל ה-`<details>` היה מקופל תמיד וללא סימן שיש בו תוכן, ולכן פתיחת סגירה שמורה נראתה כאילו איזון הקופות אבד. | הסכום מחושב מ-`r.total` ששמור בשורה (השרת מחשב אותו, לא הטופס). `test/zclosing-registers-visible.test.js`. |
| **"הוסף שטרות" / "החסר שטרות"** | `GET /zclosing`, `GET /zclosing/:id` | `public/count-adjust.js` על כל `table[data-count-adjust]` | סופרים 15 שטרות של ₪200, ואז מגיעה חבילה של 20 — לוחצים "הוסף שטרות", בוחרים שטר ומזינים 20, והכמות הופכת ל-35. בלי זה צריך לחשב בראש ולהקליד מחדש, וכל טעות חיבור הופכת לפער מזומן. החסרה נעצרת באפס ואומרת "היו רק N". | הכמויות מזוהות לפי **`input[data-val]`** בתוך הטבלה, ולכן אותה מימוש משרת גם את ספירת המגירה וגם כל בלוק ב"איזון קופות" — כולל בלוק שנוצר אחרי טעינת הדף (`window.apCountAdjust(div)` ב-`regAdd`). אחרי כל שינוי נשלח **`input` event**, אחרת סכומי הביניים והסה"כ נשארים ישנים. `test/count-adjust.test.js`. |
| הוצאות (bulk/מחיקה) | `POST /reports/zreports/:id/expenses-bulk`, `POST /reports/zexpenses/:id/delete` | `replaceExpenses`/`deleteExpense` (`zreports.js`) | הטופס משתמש ב-**`replaceExpenses`** (מחליף הכל, ממפה kind+`employee_id`/`invoice_id`). `addExpense` (append יחיד) נשאר **לטסטים בלבד** — לא מחווט ל-UI ולא ממפה `employee_id`; לא לחווט אליו טופס שכר/מפרעה. | — |
| הפקדה / כרטיסי אשראי / חשבון-מגירה | `POST /reports/zreports/:id/{deposit,creditcards,verify-bills}` | — | התאמת מזומן/אשראי; חוסר/יתרה. | `deposits.z_report_id` (nullable). |
| **מחיקת ספק** | `POST /suppliers/:id/delete` (רובריקה "מחיקת ספק" ב-`suppliers/edit.ejs`, וגם bulk ברשימה) | `deleteSupplier` (`manage_suppliers`) | מוחק ספק **ללא חשבוניות** (כולל שיוכי החנויות שלו). לספק **עם חשבוניות** — נדחה במפורש, כי מחיקה הייתה מייתמת היסטוריה כספית; הרובריקה מסבירה זאת ומציעה **חסימה** לידה (הפיכה). | הסרת בדיקת `COUNT(invoices)` = מחיקת ספק משאירה חשבוניות עם FK מת. |
| רווחיות / צ׳קים בחוץ / lookup (+CSV) | `GET /reports/{profitability,outstanding,lookup}[.csv]` | `services/reports.js` | קריאה-בלבד; `lookup` משתמש ב-`invoiceLookup` (רב-מונחי). | — |
| **דוח פדיון מידנייט** (רובריקה בדף רווחיות) | `POST /reports/profitability/revenue-import` (multer, ≤5MB) · קליטה אוטומטית: `POST /ingest/revenue-report?store=<id>&secret=…` | `lib/revenueReportFile.js` (parse) + **`lib/xlsRead.js`** (קורא `.xls` ישן: OLE2+BIFF8) → `services/revenueReports.js` (`importRevenueRows`/`revenueInRange`/`listRevenue`), טבלת **`revenue_reports`** (סכימה ×3+migrate) | הדוח הלילי (סך מכירות + סליקות אשראי) פר חנות פר יום. **מקור המכירות השיטתי** — דוחות Z נרשמים בשעות לא קבועות ולא כל יום. **שני מבנים נתמכים:** (א) **טבלה יומית** — זיהוי כותרות עברית אוטומטי (תאריך/פדיון/אשראי) + **מיפוי עמודות ידני** כשהזיהוי נכשל; (ב) **דוח מסכם יומי** (המבנה בפועל של מידנייט — JasperReports **`.xls` ישן**, לא xlsx!): שורות label→value ב-RTL (התווית בתא הימני, הערכים משמאלה), `parseSummaryGrid` שולף `סה"כ פדיון(כולל ת.חוב)` → **פדיון**, `כרטיס אשראי` → **אשראי**, `מזומן` → מזומן. **הקובץ אינו מכיל תאריך** — התאריך נבחר בטופס (ברירת מחדל: אתמול, כי המייל מגיע אחרי 00:30) או `?date=` בקליטה מהמייל. התאמת תוויות על מפתח מכווץ (בלי גרשיים/רווחים); תאריכים יום-ראשון + Excel serial; שורות כותרת מעל הכותרת האמיתית מדולגות; יום שחוזר מסוכם. ייבוא **מחליף** יום קיים (upsert על `store_id+report_date`) — שליחה חוזרת של אותו מייל לא מכפילה. | **בדף רווחיות**: עמודות "דוח פדיון" + "מזה אשראי" מסוכמות לפי טווח התאריכים, וכשיש ימים בטווח ה**רווח מחושב מהן** (`usesRevenue`); חנות בלי דוח נופלת חזרה ל-Z (`zSales`) — אין רגרסיה. הקליטה מהמייל דורשת **`REVENUE_INGEST_SECRET`** (בלעדיו 503) ומותקנת **לפני** שער ההזדהות ב-`app.js`. |
| צ׳קים בחוץ — חתך תאריך פירעון | `GET /reports/outstanding?cut=date&from&to` / `?cut=months&months[]` | `parseOutstandingCut` (route) → `dueDateCut` (`services/reports.js`) בשתי השאילתות | 3 מצבים: "הכל" / "חתך תאריכים" (from/to) / "חתך חודשים" (checkbox רב-בחירה, רק חודשים שיש בהם צ׳קים). התנאי בתוך ה-CASE על `p.payment_date` (עובד ב-pg-mem, שלא כמו WHERE על JOIN). ייצוא Excel/CSV/PDF של הטבלאות דרך כלי-הטבלה הגלובליים (`footer.ejs`). | `outstandingChecks`/`outstandingCheckDetail` מקבלים כעת `cut={month,months,from,to,storeId}`. `?month=` נשמר לתאימות. |

---

## 🔒 סגירת Z (קופה) — `routes/zclosing.js`, `services/zclosing.js`, `views/zclosing/*`

**מוגן ב-`requirePageAccess('nav_zclosing')`. קופאי נעול לדף הזה ע"י ה-firewall.**

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| הזנת סגירה + ספירת מזומן | `GET/POST /zclosing` | `createZClosing` (`computeClosing` — כל הסכומים בשרת) | **"עובד (מבצע הספירה)" הוא השדה הראשון בטופס**, מעל "איזון קופות" — נשאל **פעם אחת** לכל הסגירה. בחירת חנות ננעלת אם יש חנות מורשית אחת; אחרת dropdown scoped. חוסר>₪20 → פוש. | סדר השדות נבדק (`test/zclosing-counter.test.js` מוודא שהעובד מופיע **לפני** "איזון קופות"). חישוב תמיד בשרת (לא סומכים על הדפדפן). |
| **איזון קופות** | אותו POST (`reg_*[]`) | `normalizeRegisters` + **`stampRegisterCounter`** → `z_closings.registers` (JSON) | ספירת מזומן per-קופה לפני ה-Z; עצמאי מטבלת המגירה. **אין שדה שם בבלוק הקופה** — הסופר הוא עובד הסגירה, והשרת מטביע את שמו על כל קופה שאין לה שם משלה; הבלוק מציג אותו כטקסט (`.reg-counter`, מתעדכן חי מהבורר שלמעלה). סגירה ישנה ששמרה שם משלה לקופה **שומרת עליו** — `edit.ejs` מגלגל אותו ב-`<input type="hidden" name="reg_first[]/reg_last[]">`. | עמודה `registers` ב-3 מקומות. `edit.ejs` טוען מראש מה-JSON. הסרת ה-hidden מ-`edit.ejs` תמחק שמות היסטוריים בשמירה חוזרת. |
| הוצאות מזומן (kinds) | אותו POST (`expense_*[]`) | `insertExpenses` → `z_closing_expenses` | **עמודת "שם" היא בורר עובדים (`select.cx-payer.js-combo`), לא טקסט חופשי — בסגירת Z ובדוח Z כאחד** (`views/zclosing/index.ejs`, `views/zclosing/edit.ejs`, `views/reports/_zform.ejs` — שלושתם חייבים להישאר מיושרים, אחרת "שם" אומר דבר אחד בדף אחד ודבר אחר בשני) — הערך הנשלח נשאר `payer_name` (מחרוזת), ו-`data-emp` על ה-option מזין את `expense_employee_id` הנסתר, כך שעובד לא נבחר פעמיים; שם שמור שאינו עובד עוד מוצג כ-option "(לא ברשימת העובדים)" ולא נמחק. בלי עובדים כלל — נפילה חזרה ל-input חופשי. ידני/שכר/מפרעה/**חשבונית** (הבורר `cxKindOptsFor` ב-index/edit/_zform — הערך `invoice`, התווית "חשבונית"); מקשר עובד/חשבונית. **פריסה אחת לכל ארבעת המסכים: כרטיס מוערם (`.cx-list`/`.cx-row`/`.cx-field`), אף פעם לא טבלה** (הטכניקה השנייה בקוד לאותה בעיה היא `.line-grid` במסך הסריקה — טבלה רחבה שמתקפלת לכרטיס מתויג מתחת ל-760px דרך `data-label`; שתיהן לגיטימיות, ראו `test/scan-line-grid-layout.test.js`) — הטבלה בת 6 העמודות מעכה כל פקד לרוחב של כמה תווים בנייד, ובורר-התאריך נפתח חתוך בתוך `.table-scroll`. התיקון הוחל בזמנו על טופס ההזנה ועל דוח ה-Z אבל **דילג על `views/zclosing/edit.ejs`**, ואף טסט לא הסתכל על הפריסה — עכשיו `test/cash-expense-layout.test.js` סורק את ארבעת המסכים. חלק ההזנה בטופס עטוף ב-`<details class="cx-collapse no-collapse">` — **סגור כברירת מחדל** (המשתמש פותח כשצריך; חישוב הסכומים ב-JS רץ גם כשסגור). רובריקה משותפת בדף חשבוניות (`_cashExpenses.ejs`). ב-`_cashExpenses` עמודת **סוג** מציגה `store_name · Z <z_number>` מעל תווית ה-kind; ברובריקת "אחרונות" (לא-matchable) **כל השורה** מקבלת `class="cx-recent-row"`+`data-href="/zclosing/:closing_id"` ולחיצה עליה פותחת עריכת ה-Z — **רק לבעלים** (`GET /zclosing/:id`=`requireOwner`); לאחרים שורה רגילה בלי ניווט. לחיצה על לינק/כפתור פנימי לא מפעילה את ניווט-השורה. | `z_closing_expenses.invoice_id` (nullable, **לא** cascade — ה-clean-start מנתק אותו). ניווט-השורה תלוי ב-`closing_id`/`store_name`/`z_number` מ-`recentClosingExpenses`. `.cx-collapse`/`.cx-recent-row` מוגדרים ב-`nocturne.css`. |
| עריכה/מחיקה | `GET /zclosing/:id`, `POST /zclosing/:id[/delete]` (owner) | `updateZClosing`,`deleteZClosing` | — | — |
| "הוצאות מזומן אחרונות" / "סגירות אחרונות" (מתקפלים) | `views/zclosing/index.ejs` (client-only) | `<details class="collapse-card" data-accordion="recent-z">` + סקריפט accordion inline | שני קלפים מתקפלים, סגורים כברירת מחדל; פתיחת אחד סוגרת את השני (קבוצת `recent-z`). תצוגה בלבד — לא נשמר. | הסקריפט תלוי ב-`data-accordion` על שני ה-`<details>`; להשאיר אותם באותה קבוצה. `.collapse-card` מוגדר ב-`nocturne.css`. |



---

## 🏦 העברות בנקאיות — `routes/transfers.js`, `services/transfers.js`, `views/transfers/*`

**🔴 השליטה האמיתית כבר קיימת — בבנק.** למזכירה יש שם משתמש משלה; היא בונה את המקבץ ו**שום כסף לא זז** עד שהבעלים משחרר אותו בבנק. הדף הזה **לא מנסה לשכפל את המנעול הזה** (זה היה עותק גרוע של בקרה שכבר עובדת). מה שחסר הוא כל מה **סביב** האישור: היום הוא ניתן במסך הבנק כשההקשר היחיד הוא תמונה בוואטסאפ. **הדף הוא התדריך לאישור שקורה במקום אחר**, ותיעוד ההחלטה.

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| **בקשת העברה (לפני הבנק)** | `POST /transfers` | `createTransfer` → `bank_transfers` + `bank_transfer_lines` | **שום דבר כספי לא מוקלד:** מסמנים חשבוניות, והסכום/ספק/חנות/חשבון **נגזרים מהן**. מוצעות רק חשבוניות `approved_for_payment` שטרם שולמו, של ספק מאושר, בתוך הסקופ — זה הכלל "רק מול חשבונית שלא שולמה", והשירות בודק מחדש כל id. **העברה אחת = ספק אחד + חנות אחת** (אחרת אין משמעות לאישור), וחשבונית לא יכולה להיות בשתי בקשות פתוחות. `opened_at` בשעון ישראל = העוגן לבדיקת back-fill. | הבורר מציע רק מה שמותר, אבל ה-route מריץ `assertInScope('invoice')` על כל id — הוא חוזר דרך טופס. |
| **אישור הבעלים — לפני שחרור המקבץ בבנק** | `POST /transfers/:id/approve` (**`requireOwner`**) | `approveTransfer` | הפעולה האנושית **היחידה** בזרימה. בשורה עצמה: החשבונית במרחק לחיצה **ופרטי הבנק של הספק** — זה התדריך שמחליף את התמונה בוואטסאפ, והבעלים משווה אותו למסך הבנק לפני שהוא משחרר שם. רישום ביצוע לפני אישור נדחה. | הסרת `requireOwner` הופכת את כל הדף לחסר משמעות. |
| **🔴 פרטי בנק של ספק (יעד ההעברה)** | `POST /suppliers/:id/bank` · `POST /suppliers/:id/bank/:accountId/verify` · `.../delete` (כולם **`requireOwner`**) | `services/suppliers.js#setSupplierBankAccount` / `verifySupplierBankAccount` / `deleteSupplierBankAccount` / **`supplierBankFor`** → טבלת **`supplier_bank_accounts`** + **`supplier_bank_changes`**; אימות ורשימת בנקים ב-**`lib/banks.js`**; תצוגה ב-`views/suppliers/edit.ejs` | **ההעברה המסוכנת אינה חשבונית מזויפת — היא חשבונית אמיתית, בסכום נכון, שאושרה כראוי, ששולמה לחשבון שהוחלף בשקט.** לכן: **שורה לכל (ספק, חנות)** — `store_id` ריק = ברירת מחדל, וחשבון של חנות **גובר** עליה; הבנק נבחר **מרשימת קודי בנק ישראל** (`BANKS`) והשם נגזר מהקוד ולא מוקלד; סניף/חשבון/**ח״פ מוטב (ספרת ביקורת)**/**IBAN (mod-97)** מאומתים; שינוי הוא **פעולת בעלים**, נשמר עם מי ומתי + **פוש**, **ומאפס את האימות הטלפוני**. שורת ההעברה מציגה את היעד ואת "פרטי הבנק שונו לאחרונה" (`BANK_CHANGE_WARN_DAYS` = 60) **ליד כפתור האישור**. | **`supplierBankFor` היא ההכרעה היחידה "לאן הכסף הולך"** — גם המסך, גם `substanceOf` וגם הבדיקה שלפני הביצוע קוראים לה; שאילתה מקבילה תיצור מסך שמראה חשבון אחד וכסף שהולך לאחר. **אימות חוסם רק מה שבוודאות שגוי**; שם מוטב שאינו תואם **מזהיר ונשמר** — חסימה הייתה מוציאה את פרטי הבנק מהמערכת. `suppliers.bank_*` הן **עמודות מת** שנשארו רק כדי לא להסיר עמודה ממסד חי; העדכון מעביר מהן שורת ברירת מחדל, **בלי קוד בנק** (`guessBankCode` מציע, הבעלים מאשר). סכימה ×3+migrate. `test/supplier-bank.test.js`. |
| **רישום ביצוע (אסמכתה)** | `POST /transfers/:id/execute` | `executeTransfer` → **`createPayment`** רגיל | **השדה המוקלד היחיד בכל הזרימה**, ורק אחרי אישור. יוצר תשלום `method='transfer'` בנתיב הרגיל, ולכן R1/R5, סטטוס החשבונית וההתאמה הבנקאית מתנהגים בדיוק כמו בכל תשלום אחר. | אין סטטוס `cleared` בעמודה — ראו למטה. |
| **סטטוס — אף אחד לא מסמן לעצמו "נפרע"** | `listTransfers#displayStatus` | `payments.status` | `pending` → `approved` → `executed` → **`cleared` מחושב** מכך שהתשלום נפרע, כלומר **מהבנק**. הערך `cleared` לא קיים ב-CHECK של העמודה, ולכן אי אפשר לכתוב אותו ידנית בכלל. גם `rejected`/`cancelled`. | הפיכת `cleared` לסטטוס מאוחסן שוברת את ההבטחה. |
| **🔴 מעקב אחר העברות ללא תיעוד** | רובריקה אדומה ב-`/transfers` + באנר בלוח הבקרה; פוש מ-`autoReconcile` ומהקרון `GET /ingest/voided-checks` | `untrackedTransfers` + `alertOnUntrackedTransfers` | **לא בגלל שהמזכירה יכולה לשלם לבד — היא לא יכולה.** אלא כי אישור שניתן בבנק בלי בקשה כאן פירושו שהתדריך דולג, וזה בדיוק המצב שהדף נועד למנוע. נקרא מדף החשבון של הבנק ולא ממה שמישהו הקליד: חיוב לא-מותאם, שאינו נושא אסמכתה מוכרת, ואינו צ׳ק (צ׳קים נעקבים לפי מספרם בדף שלהם). | **`transfer_watch_from` ב-`app_settings` — חובה, ומכוון:** הכלל **צופה קדימה בלבד**. בלי תאריך התחלה כל העברה היסטורית הייתה מתריעה, והתראה שיורה על היסטוריה היא התראה שלא קוראים. כבוי כברירת מחדל. אידמפוטנטי דרך `transfer_alerted_ids`. |
| **🔴 אישור לא שורד שינוי במה שאושר** | `substanceOf` + `approved_fingerprint` | נבדק ב-`executeTransfer` וב-`listTransfers` | האישור הוא של **ספק מסוים, סכום מסוים, חשבוניות מסוימות ויעד מסוים**. בזמן האישור נשמרת טביעת-אצבע של כל אלה; לפני ביצוע היא מחושבת מחדש, וכל שינוי מבטל את האישור (`stale`). **זו לא "נעילת עריכה"** — המהות משתנה גם בלי שאף אחד עורך את הבקשה, והמקרה שחשוב הוא **שינוי חשבון הבנק של הספק אחרי האישור**, בדיוק התרמית שהיעד נועד לה. נעילת שורה לא הייתה תופסת את זה; השוואת טביעת-אצבע כן. | אישור מחדש = החלטה חדשה על העובדות הנוכחיות, ולכן לוקח טביעת-אצבע חדשה ומאפס `alerted`. |
| **פקיעת אישור** | `approvalExpired` + `config.rules.transferApprovalTtlDays` (7) | `executeTransfer` דוחה, הדף מציג `expired` | אישור פתוח הוא רישיון עומד לשחרר כסף בבנק. מקבץ משוחרר בדרך כלל באותו יום או למחרת, ולכן אחרי 7 ימים האישור פג ונדרש אישור מחדש. | הכפתור הופך ל"אשר מחדש" וטופס רישום הביצוע נעלם עד שיאושר. |
| **🔴 סכום שונה בבנק** | `listTransfers#amountMismatch` | תנועת הבנק המותאמת ל-`payment_id` של הבקשה | הבנק הוציא סכום אחר מזה שאושר — שחרור חלקי, עמלה במקור, או מקבץ שנערך בבנק **אחרי** התדריך. הסטטוס `mismatch` **גובר על "נפרע"**, כדי שזה לא ייסגר בשקט. | סכום זהה נסגר רגיל — נבדק שאין התראת שווא. |
| **התראות על שלושת המצבים** | `alertOnTransferProblems` (מ-`autoReconcile` ומהקרון) | `lib/notify.js` | פוש + פעמון על `expired` / `stale` / `mismatch`. אידמפוטנטי דרך `bank_transfers.alerted` (הסוג האחרון שנשלח). | אישור מחדש מאפס `alerted`, כך שבעיה חדשה על אותה בקשה עדיין תדווח. |
| **זיהוי back-fill** | `backfilled(transfer, txnDate)` | `opened_at` מול תאריך התנועה | בקשה שנפתחה **אחרי** שהכסף כבר זז = תיעוד בדיעבד. לא האשמה בפני עצמה (דף חשבון מיובא באיחור), אבל זה הדבר היחיד שנוהל לא יכול לתפוס לבד, ולכן הוא מוצג ולא מוסתר. | תלוי ב-`opened_at` בשעון ישראל. |
---

## 🟠 חשבוניות מעוקבות לתשלום — `routes/trackedInvoices.js`, `services/trackedInvoices.js`, `views/tracked/index.ejs`

**מוגן ב-`requirePageAccess('nav_tracked_invoices')`.** חשבוניות שמחכות לטיפול של הספק — זיכוי שהובטח, כמות שלא סופקה, מסמך חסר, מחיר שגוי — לפני שסוגרים אותן.

| פעולה | route | wiring | מה קורה | תלות/שבירה |
|------|-------|--------|---------|-------------|
| **"🟠 עקב תשלום" בטופס חשבונית חדשה** | `POST /invoices` עם `action=track` | `routes/invoices.js` → `trackInvoice` | נשמרת כרגיל **ובנוסף** מסומנת; מפנה לדף המעוקבות. הכפתור יושב **בין "שמור והוסף עוד לספק" לבין "ביטול"**. | הסימון עטוף ב-try/catch: לפני עדכון המסד החשבונית כבר נשמרה, ואסור להפיל את הבקשה — נופל ל-`?trackfail=1`. |
| **דף המעוקבות** | `GET /tracked-invoices` | `listTracked({scope, filters})` | עמודות **כמו דף החשבוניות** עם שתי החלפות שהבעלים ביקש: במקום "חנות" → **סטטוס**, במקום "שולם" → **הסבר / פירוט** (שדה עריכה inline). חיפוש בראש הדף (ספק · מספר · סכום מדויק · טווח תאריכים) **בשרת**, ובנוסף סינון לכל עמודה + מיון בטבלה (`sortable filterable`, `partials/footer.ejs`). מספר חשבונית = `inputmode="numeric"` וסכום = `decimal` → **מקלדת מספרים בנייד**. | `trackedReady` שומר על הדף לפני עדכון המסד. הטבלה `line-grid` עם `data-label` — בנייד כרטיס ולא טבלה מעוכה. |
| **שליחה לספק (וואטסאפ / מייל)** | קישורי `wa.me` / `mailto:` | `trackedMessage(inv)` + `whatsappLink` / `mailtoLink` | ההודעה **נגזרת ממה שהוזן** — ספק, מספר, תאריך, סכום ו-`tracked_note` — ולא מוקלדת מחדש. בלי הערה עדיין נשלחת הודעה שלמה. טלפון ישראלי מומר לפורמט בינלאומי; **בלי טלפון** הקישור הוא `wa.me/` בלי מספר, כך שוואטסאפ מבקש לבחור איש קשר במקום להיכשל. | שלוש הפעולות הן **אייקונים** (`.icon-btn`) ולא מילים — במילים הטבלה גלשה מעבר לרוחב הכרטיס ונחתכה (נמדד: 1106px בתוך 940px). המשמעות ב-`title`+`aria-label`. |
| **שחרור מעיקוב** | `POST /tracked-invoices/:id/release` | `releaseInvoice` | מוריד את הדגל → החשבונית **נעלמת מהדף**. `tracked_note` **נשמר** — מה שנדרש נשאר קריא גם אחרי שהסתיים. | `assertInScope('invoice', …)` לפני כל פעולה. |
| **שורה כתומה בדף החשבוניות** | `GET /invoices` | `views/invoices/index.ejs` → `row-tracked` + באדג' `b-tracked` | כתום גובר על האדום של `on_hold`: זו בעיה מולי ומול הספק, וזה מה שדורש פעולה. **צבע לבדו אינו סטטוס** — לצדו באדג' "מעוקבת לתשלום" במילים. | — |

**🔴 למה דגל (`invoices.tracked_for_payment`) ולא ערך ב-`status`:** `on_hold` **מנוהל אוטומטית** — `updateInvoice` מדליק ומכבה אותו לפי R3 בכל עריכה, ושיוך תשלום מנקה אותו. עיקוב שהיה יושב ב-`status` היה נמחק בשקט ברגע שמישהו מתקן שדה בחשבונית. בנוסף, עיקוב הוא **תזכורת מול הספק ולא חסימה טכנית**: חשבונית יכולה להיות מאושרת לתשלום, ואפילו משולמת, ועדיין לחכות לזיכוי — ואת זה `status` לא יודע לבטא. הדגל נדלק ונכבה רק בידיים. סכימה ×3 (`tracked_for_payment`/`tracked_note`/`tracked_at`/`tracked_by`) + migrate. `test/tracked-invoices.test.js`.

**רשימות צפות** (`.combo-pop` של `js-combo`, `.dp-pop` של בורר התאריך) — `position: fixed`, ההורה נבחר ברגע הפתיחה ב-`apPopupHost` (`partials/footer.ejs`): דיאלוג פתוח שמכיל את השדה, אחרת `<body>`. **absolute נחתך** בידי כל אב עם `overflow` (בתוך `.table-scroll` הרשימה נגזרה), ו-**fixed על `<body>` נקבר מתחת ל-`<dialog>`** מודאלי ואינו ניתן ללחיצה בשום z-index. רוחב הרשימה מוצמד ל-240px לפחות — בטבלה בת 7 עמודות בנייד השדה יוצא ברוחב 33px. `test/popup-anchoring.test.js`.

**סדר התפריט** נקבע ע"י הבעלים ונעול ב-`test/nav-order.test.js`: בנק (התאמת בנק → העברות → צ׳קים מבוטלים → יומן), אחר כך ספקים → עובדים ומשכורות → רווחיות, המעוקבות ליד החשבוניות, וצילום חשבוניות מעל הגדרות. דף חדש נוטה להידחף לסוף הרשימה ולפרק את הקיבוץ — הטסט מונע את זה.

---

## 🚫 צ׳קים מבוטלים + תשלומי שכר — `routes/voidedChecks.js`, `services/voidedChecks.js`, `services/salaryPayments.js`

**🔴 ביטול צ׳ק במערכת אינו מבטל אותו בבנק.** צ׳ק נשאר בר-פירעון **182 יום** (`CHECK_LIFE_DAYS`) מתאריך הפירעון, ולכן צ׳ק שבוטל נשאר במעקב עד שהוא בטוח. זו כל הסיבה לקיומו של הדף.

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| **דף צ׳קים מבוטלים** (רובריקה לכל חנות) | `GET /voided-checks` (`nav_voided_checks`) | `listVoidedChecks({scope})` | עמודות: תאריך ביטול · מספר צ׳ק · עבור · לתאריך · סכום · סיבת ביטול · בוטל ע"י · **סטטוס**. "עבור" נגזר מ-`payment_lines` (ספק·חשבונית) או מ-`salary_payments` (שכר·עובד). קיבוץ לפי `bank_accounts.store_id`. | **סובלנות לפני שדרוג:** `voidedChecksReady`/`salaryPaymentsReady` בודקות שהעמודות/הטבלה קיימות; עד שהבעלים מריץ "עדכן מסד נתונים" הדף מציג משפט שאומר מה לעשות במקום `column p.voided_by does not exist`. אותו דפוס כמו `filterByStoreLinks`. |
| **סיבת ביטול — נבחרת, לא מוקלדת** | `POST /payments/:id/void` (`void_reason`) | `payments.js#voidPaymentWithReason` → `payments.void_reason/voided_at/voided_by/void_link_*` | ארבע סיבות, לכל אחת מעקב משלה: **לא נאסף** (בטוח רק אחרי 182 יום) · **נפרע במזומן עבור שכר** (חובה התאמה להוצאת מזומן) · **שינוי אמצעי תשלום** (חובה קישור לתשלום החדש) · **ביטול שורה בתוכנה** (חובה קישור לשורה). | טקסט חופשי לא ניתן למעקב — לכן הסיבה היא enum ב-CHECK constraint (סכימה ×3+migrate). `voidPayment` הישן עדיין עובד (ביטול בלי סיבה) ופשוט אין לו מעקב. |
| **עמודת הסטטוס** | `voidedChecks.js#checkStatus(row, today)` | טהורה — נבדקת ישירות | `cashed` (🔴 נפרע אחרי הביטול — גובר על הכל) · `unmatched`/`unlinked` (המעקב שהסיבה דורשת לא בוצע) · `live` (עדיין בר-פירעון, עם התאריך) · `expired` (עבר תוקף — בטוח). | זו העמודה החשובה בדף. שינוי הסדר משנה איזו בעיה מוצגת. |
| **זיהוי "נפרע אחרי הביטול"** | בכל `autoReconcile` (ייבוא דף חשבון / התאמה אוטומטית / `POST /reconciliation/sync`) | `listVoidedChecks` + `reconciliation.js#voidedCheckHits` | שתי ראיות: `matched_payment_id` מפורש, **או** חיוב לא-מותאם באותו חשבון, באותו סכום, שנושא את מספר הצ׳ק. שתיהן מיושמות גם ב-`listVoidedChecks` כדי שהדף וההתראה לא יסתרו. | סכום שונה או חשבון שונה = **לא** אותו צ׳ק (נבדק). |
| **קישור אחרי הביטול** | `POST /voided-checks/:id/link` | `voidLinkOptions` (מציע) + `setVoidLink` (שומר) | **זה הסדר האמיתי:** אי אפשר לרשום תשלום חלופי כל עוד החשבונית משולמת ע"י הצ׳ק, ולכן "שינוי אמצעי תשלום" הוא תמיד **ביטול → הנפקה מחדש → קישור**. הבורר בדף הביטול מציע רק מה שכבר קיים; הקישור החסר מושלם מדף צ׳קים מבוטלים, בשורות שהסטטוס שלהן "ללא קישור". המועמדים נגזרים **מהצ׳ק עצמו** (החשבוניות שלו והתשלומים החיים עליהן / על אותו ספק) — אי אפשר להצביע על שורה זרה. | כל `link_*_id` עובר `assertInScope` (הבורר מציע רק שלנו, אבל ה-id חוזר דרך טופס). `setVoidLink` מאפס `void_alerted` כדי שהשורה תיבדק מחדש בסריקה הבאה. |
| **התראות** | `alertOnVoidedChecks` + `alertOnExpiredNotCollected` (נקראות מ-`autoReconcile` **וגם מקרון לילי** `GET /ingest/voided-checks`, `CRON_SECRET`, `vercel.json` 05:30) | `lib/notify.js` (טלגרם + פעמון) | פוש על: צ׳ק שבוטל ונפרע · "נפרע במזומן עבור שכר" בלי התאמה · חסר קישור · ו"לא נאסף" שהגיע ל-182 יום. **אידמפוטנטי** דרך `payments.void_alerted` (הסטטוס האחרון שנשלח), כך שהרצה חוזרת שקטה עד ששינוי אמיתי קרה. | **הקרון הוא לא כפילות:** שתי סיבות הן **דדליין ולא מצב** — "לא נאסף" נעשה בטוח בתאריך, וחוסר התאמה/קישור רק מחמיר עם הזמן. אף פעולת משתמש לא תבחין בזה, וקבוצה שלא ייבאה דף חשבון שבועות לא הייתה שומעת כלום. `try/catch` סביב הקריאה מ-`autoReconcile` — התראה לא מפילה התאמה. איפוס `void_alerted` ב-`voidPaymentWithReason` מאפשר התראה מחדש אחרי ביטול-מחדש. |
| **באנר אדום בלוח הבקרה** | `GET /` | `voidedChecksSeenInBank(scope, storeId)` → `views/dashboard.ejs` | "🔴 צ׳ק שבוטל נפרע" עם **קישור לצ׳ק** וקישור לדף צ׳קים מבוטלים. | תלוי ב-`h.payment.id` מ-`voidedCheckHits`. |
| **רובריקת תשלומי שכר** | `GET /employees`, `POST /employees/salary` | `salaryPayments.js#createSalaryPayment` → `salary_payments` | עמודות: שם העובד · שולם ב (צ׳ק/מזומן/העברה/מקבץ) · מס׳ אסמכתה · לתאריך · סכום ₪. בורר העובדים מציע **רק עובדי החנות** (עובד ללא שיוך = משותף). התאריך נכתב גם ידנית (8/9/26) דרך בורר-התאריך הגלובלי. צ׳ק/העברה/מקבץ **חייבים אסמכתה**; מזומן לא. | `store_id` מהבקשה עובר `assertStoreAllowed`. "הוספת עובד" הפכה ל-`<details>` **מצומצם כברירת מחדל** — הרובריקה היומיומית למעלה. |
| **מפרעות והלוואות + מעקב החזרים** | `GET /employees` · `POST /employees/advances` · `.../advances/:id/repay` · `.../repay/:repaymentId/delete` · `.../advances/:id/delete` | `services/employeeAdvances.js` → **`employee_advances`** + **`employee_advance_repayments`** | עמודות: תאריך · עובד · סוג (מפרעה/הלוואה) · ניתן ב · חנות · סכום · **הוחזר · יתרה · סטטוס** (פתוח/חלקי/הוחזר), + טבלת חוב פתוח לכל עובד. **הרובריקה הישנה קראה רק שורות מפרעה מדוח Z** — כלומר כסף שיצא מהקופה; מפרעה שניתנה בהעברה/צ׳ק/מהכיס לא הופיעה בכלל, ולהחזר לא היה איפה להירשם. עכשיו מפרעה נרשמת ידנית עם אמצעי התשלום שלה, וכל החזר (ניכוי מהשכר / מזומן / אחר) מוריד את היתרה; החזר אפשר לתלות ב-`salary_payments` שממנו נוכה. | **מפרעה מ-Z משוקפת לכאן** ב-`syncZAdvances` (רץ בטעינת הדף), אידמפוטנטי דרך `z_expense_id UNIQUE` — בלעדיו מפרעה שנרשמה בקופה אתמול לא הייתה ניתנת להחזר היום. **ה-Z הוא הבעלים של הסכום:** תיקון ב-Z מעדכן את השורה כאן, לא להפך, ומחיקה כאן חסומה (הייתה חוזרת בסנכרון הבא). שורת Z שנמחקה מוסרת — **אלא אם נרשמו עליה החזרים**, כי החזר הוא עובדה שקרתה. **יתרה מחושבת ולא נשמרת** (אין עמודת "הוחזר" ואין דגל "סגור"), ולכן לא יכולה להתנתק מהשורות. החזר מעל היתרה, החזר שלילי, והחזר שנתלה בשכר של עובד אחר — כולם נדחים. `store_id` עובר `assertStoreAllowed`, והרשימה עוברת `scopeWhere`. `advancesReady` שומר על הדף לפני העדכון. `test/employee-advances.test.js`. **הפעולה היא כפתור בכותרת הרובריקה ו‑`<dialog id="add-advance">`, וגם במצב הריק** — בגלגול הראשון היא הייתה `<details>` דק בתחתית הכרטיס ואיש לא מצא אותה; הטבלה (10 עמודות) מתקפלת לכרטיס מתויג בנייד דרך `.line-grid`+`data-label`, והדיאלוגים ממקדים `<h3 tabindex="-1" autofocus>` כי אחרת `js-combo` תופס פוקוס, נפתח ומכסה את התאריך והסכום. `test/advance-form-layout.test.js`. |
| **"הצ׳ק נפרט" — התאמה למזומן** | `POST /employees/salary/:id/cashed` | `salaryPayments.js#markCashed` → `voidPaymentWithReason('cashed_for_salary')` | העובד פרט את צ׳ק השכר בקופה: הכסף יצא מהקופה (הוצאת מזומן בסגירת Z), ולכן הצ׳ק **חייב להתבטל** — אחרת ייפרע בבנק וישולם אותו שכר פעמיים. ההתאמה מציעה הוצאות מזומן **פנויות** בלבד; הוצאה שכבר שויכה לא מוצעת שוב, ושיוך כפול נחסם. | **סדר קריטי:** מבטלים קודם ואז מסמנים. `voidPayment` פותח טרנזקציה משלו — עטיפה ב-`tx()` נוספת זורקת "cannot start a transaction within a transaction", וגם ביטול שנדחה היה משאיר את שורת השכר מסומנת מול צ׳ק חי. |
---

## 🏢 ספקים — `routes/suppliers.js`, `services/suppliers.js`, `views/suppliers/*`

| כפתור/פיצ'ר | route | wiring | מה עושה / מקושר | אם מוחקים/משנים |
|---|---|---|---|---|
| רשימה / אנשי קשר / חדש / עריכה | `GET /suppliers[/contacts,/new,/:id/edit]`, `POST …` | `createSupplier`,`updateSupplier` | סטטוס pending/approved/blocked; שיוך רב-חנותי (`supplier_stores`, `_storepick.ejs`). | חשבוניות/תשלומים תלויים ב-`supplier.status='approved'` (R1). |
| **חברת-אם (תשלום מרוכז)** | טופס `GET/POST /suppliers/:id/edit` (שדה `parent_supplier_id`) | `updateSupplier`(`parentSupplierId`)→`validateParent`; `supplierFamilyIds(id)` | חברת-בת (טרה) מצביעה לחברת-אם (קוקה קולה) דרך עמודת **`suppliers.parent_supplier_id`** (סכימה ×3+migrate; `getSupplier`/`listSuppliers`=`SELECT *` אז צף לבד). **מגבלה: רמה אחת** — האם חייבת להיות top-level, לא עצמי, וספק שהוא-אם לא יכול לקבל אם. `supplierFamilyIds`=root(אם/עצמי)+כל הבנות → `batchContext` וה-picker (`data-fam`) כוללים את כל המשפחה, כך שחשבוניות של שתיהן (אותה חנות) משולמות בתשלום אחד. | `createPayment` כבר מתיר חוצה-ספק אותו חשבון-בנק. הסרת העמודה שוברת את הבורר. **תזכיר לבעלים "עדכן מסד נתונים".** |
| אישור/חסימה/מחיקה | `POST /suppliers/:id/{approve,block,delete}` | — | אישור = מאפשר תשלום. | חסימה חוסמת תשלום. |
| "סקיל" הספק | `POST /suppliers/:id/scan-hints` | `services/supplierProfile.js` (`suppliers.scan_profile`) | פרופיל מבנה החשבונית לצילום. | אזור סשן מקביל — תאם. |

---

## 🔐 הפרדת חנויות — מה בדיוק מגן, ואיפה זה נבדק

**🔴 `POST /context/store` מחזיר 303 (See Other) ולא 302, וכפתור "החלף" עובר דרך `apAutoSubmit`
כמו ה-select.** 302 על POST מאפשר לסוכן לשלוח את הבקשה **שוב כ-POST** ליעד; ב-PWA המותקן זה גרם
לכך שהלחיצה על "החלף" פשוט לא ניווטה — האפליקציה נראתה תקועה. ה-select תמיד עבר דרך fetch (204,
בלי redirect בכלל) והכפתור לא — לכן זה קרה רק בכפתור. שני התיקונים מכוסים בטסטים; אל תחזיר ל-302.

### 🔒 נעילה הרמטית — כלל אחד לכל דף ולכל תצוגה, גם לדפים שעוד לא נכתבו

**בחירת סניף מצמצמת את `req.scope` עצמו — היא לא "ברירת מחדל" של בורר.** זה קורה במקום **אחד**:
`middleware/currentUser.js`, אחרי חישוב `activeStore` — `req.scope` הופך ל-`{companyIds:[חברת
החנות], storeIds:[החנות], activeStoreId}`. מכאן כל צרכני הסקופ אוכפים את הנעילה **בלי שורת קוד
בדף**: `scopeWhere`/`scopeClause` בכל שירות-רשימה, `scopedStoreList` בכל בורר חנות,
`filterByStoreLinks` לספקים/עובדים, `assertInScope` לדפי-id ו-`assertStoreAllowed` לכל כתיבה.
**דף חדש יורש את הנעילה ברגע שהוא משתמש בסקופ** — וזה בדיוק מה שנדרש: "כלל לכל דף, גם עתידי".

- **פרמטר חוצה-סניף לא מכובד, מתעלמים ממנו.** `?store=`/`?zstore=`/`?account=` שמצביע על סניף
  אחר לא מציג אותו — קישור ישן, סימנייה או בורר שנשאר על ערך קודם לא יכולים להעלות כסף של סניף
  אחר על מסך שהבאנר שלו אומר אחרת. `lib/scope.js#effectiveStoreId(req, requested)` הוא הביטוי
  המפורש של זה בדפים שמקבלים פרמטר חנות. **"כל החנויות" (ניקוי הבורר) היא הדרך היחידה להסתכל
  רוחבית.**
- **דף-id של סניף אחר = 404**, לא רק "לא ברשימה" (`assertInScope`).
- **החריג היחיד — `lib/scope.js#assignmentScope(req)`** (מחזיר את `req.grantedScope`, ההרשאות
  הלא-מצומצמות): בוררי **שיוך** ספק/עובד לחנות ("העתק לחנות"). זו הרשאה ניהולית שחושפת רק שם
  חנות שהמשתמש כבר מורשה לה — לא נתונים של סניף אחר. **אין להשתמש בו לרשימה, לסיכום או לדף
  פרטים.** דפי `/settings` (ניהול הארגון — שם *יוצרים* את הסניפים) ו-`/notifications` מחוץ
  לכלל במפורש, ראו `EXEMPT` בטסט.
- **הבדיקה נגזרת מהאפליקציה, לא מרשימה ידנית:** `test/active-store-lock.test.js` הולך על
  `app._router.stack`, אוסף **כל** route של GET, וסורק את כולם עם סניף אחד נבחר — כל סימן של
  סניף אחר (מספר חשבון בנק, מספר חשבונית, שם ספק, שם עובד) = כישלון. דף שייווסף מחר מכוסה
  אוטומטית ברגע שהוא נטען. אם הנעילה מבוטלת הטסט נופל על `/audit`, `/suppliers`, `/reconciliation`
  ועוד — נבדק.

מכוסה גם ב-`test/store-context.test.js` (הבאנר, הנעילה האוטומטית לחנות יחידה, והסינון בפועל).

**הכפתור "החלף" (חנות פעילה) הוא נוחות, לא הגנה.** הוא רק בוחר *מסנן* מתוך `availableStoresFor(user)`;
הוא לא יכול להרחיב גישה, וזיוף עוגיית `ap_store` לחנות לא-מורשית פשוט מתעלמים ממנו
(`middleware/currentUser.js` — הערך נבדק מול הרשימה המורשית). **הגבול האמיתי הוא `req.scope`.**

שלוש שכבות, וכולן חייבות להתקיים:
1. **רשימות** — `scopeWhere(scope, companyCol, storeCol)` בשירות.
2. **גישה לפי id** — `assertInScope(kind, id, req.scope)` ב-route (404, לא 403 — לא מדליף קיום).
3. **🔴 `store_id` שמגיע מהבקשה** — **`assertStoreAllowed(storeId, req.scope)`** (`lib/scopeGuard.js`) *לפני כל כתיבה*. שכבות 1–2 שומרות על מה שרואים; זו שומרת על מה **שכותבים**. בלעדיה משתמש עם הרשאה לחנות א׳ יכול לשלוח `store_id` של חנות ב׳ בטופס ולהכניס לשם חשבונית/דוח/סגירה.

**🔴 `scopeClause` פולט `AND NOT (col NOT IN (…))` ולא `AND col IN (…)` — אל "תפשטו" בחזרה.**
שקול לחלוטין ב-SQL (NULL נופל בשני המקרים, והרשימה לעולם לא מכילה NULL) ו-Postgres מתכנן אותו
דבר, אבל **pg-mem** (הדיאלקט שהטסטים רצים מולו ב-`TEST_PG=1`) זורק "Not supported: lookups on
joins" על `… AND <id מאונדקס של טבלה ב-JOIN> IN (…)` — בדיוק הצורה שנוצרת ל-`st.id`/`ba.store_id`
בכל בורר חנות ורשימת חשבונות. חזרה ל-`IN` מאדימה את `TEST_PG=1` ב-`/audit`, `/zclosing`,
`/reconciliation` ו-`/reports/*`.

**בוררי חנות — תמיד `scopedStoreList(scope)`** (`lib/scope.js`, מסנן חברה **וגם** חנות ומחזיר `company_id`). אותה שאילתה לא-מסוננת שוכפלה לארבעה routers והדליפה את כל החנויות בארגון לתוך dropdown. אין לכתוב `SELECT … FROM stores JOIN companies` ישירות ב-route.

**רמת החברה:** `test/company-isolation-attack.test.js` (11 מקרים) — משתמש עם הרשאה ל**חברה שלמה** (כל חנויותיה, בלי הרשאת-חנות) תוקף חברה שנייה: קריאה, כתיבה, ביטול תשלום, משיכה על חשבון בנק זר, יצירת חנות בחברה זרה, ויומן הביקורת. שם הוצפו שתי דליפות: **`listZReports` היה ללא סינון בכלל** (כל דוחות ה-Z בארגון) ו-**יומן הביקורת** (`listRecent`) חשף מספרי צ׳קים וחשבוניות של חברות אחרות. שניהם מסוננים כעת; ב-`listRecent` הסינון הוא **fail-closed** — רשומה שלא ניתן לייחס לחברה (user/company/supplier/system) לא מוצגת למי שאינו בעלים.

**ספקים ועובדים — הפרדה דרך שיוך לחנויות** (`supplier_stores` / **`employee_stores`**, סכימה ×3+migrate). כלל אחד לשניהם ב-**`lib/scope.js#filterByStoreLinks`**:
- **ללא שיוך = משותף לכולם** (מה שכל השורות היו לפני שהשיוך היה קיים, ומה שספק/עובד ארגוני באמת הוא). **לעולם לא מוסתר** — כך שום דבר לא נעלם בשדרוג.
- **עם שיוך** = נראה רק היכן שלפחות חנות אחת מהשיוך נמצאת בסקופ.
- שורה יכולה להיות משויכת ל**כמה חנויות בכמה חברות** — כך "אותו ספק מוריד סחורה לשני הסניפים" ו"אותו עובד עובד בשתי חנויות" עובדים: **שורה אחת, שני שיוכים**, לא שכפול.
- כל שורה מקבלת מערך **`stores`** → זה **סימן הזהות בממשק**: צ'יפים עם שמות החנויות בדף העובדים (עמודה "חנויות") ובדף הספקים; "כל החנויות" כשאין שיוך. שינוי/העתקה: `POST /employees/:id/stores` (רובריקת "שנה / העתק לחנות") ו-טופס עריכת הספק — שניהם דרך `_storepick.ejs` המשותף.
- **🔴 כתיבה מוגנת:** כל `store_ids` שנשלח נבדק ב-`assertStoreAllowed` לפני שמירה, אחרת משתמש היה יכול לשייך ספק/עובד לחנות שאינו רואה (ודרכה לראות אותה).
- **גיבוי המיגרציה:** `employee_stores` מאותחל **פעם אחת בלבד** (רק כשהטבלה ריקה) מהמקומות שבהם העובד באמת מופיע — `z_closings`, `z_expenses`, `z_closing_expenses`. עובד שלא מופיע בשום מקום נשאר ללא שיוך = משותף.

**⚪ עדיין ללא ממד חברה (החלטת מוצר):** `calendar_events` וקטלוג המוצרים — משותפים לכל הארגון בכוונה.

**`test/store-isolation-attack.test.js`** הוא הבדיקה האדוורסרית (17 מקרים, HTTP אמיתי): משתמש עם הרשאה לחנות **אחת** מנסה לקרוא ולכתוב אצל (א) חנות בחברה אחרת, ו-(ב) **חנות אחות באותה חברה** — המקרה הקשה, שבו כל בדיקות החברה עוברות ורק ממד החנות עוצר. כל route חדש שנוגע ב-`store_id` — הוסף לו מקרה שם.

---

## 🏦 התאמת בנק — `routes/reconciliation.js`, `services/reconciliation.js`

**מוגן ב-`requirePageAccess('nav_reconciliation')`.** ייבוא CSV/XLSX (Hapoalim, `lib/bankCsv.js`), התאמת תנועות לתשלומים (`bank_transactions.matched_payment_id`) והפקדות (bag=reference). מחיקת תשלום מנתקת התאמות (clean-start עושה זאת).
- **הפרדת חברות ב-POST (נגד IDOR):** `resolveAccountId` הוא שער-הסקופ היחיד ל-`account_id` — מאמת את ה-id (מ-body או query) מול החשבונות המורשים ל-`req.scope.companyIds`, ו-id מזויף/זר נופל לחשבון המורשה הראשון (לעולם לא פעולה חוצת-חברה). כל handler שמקבל `txn_id`/`:id`/`payment_id` קורא `assertInScope('bankTxn'|'payment', …, req.scope.companyIds)` **לפני** הפעולה (404 מסתיר קיום). שני kinds חדשים ב-`scopeGuard.js`: `bankAccount`, `bankTxn`.
- **🔴 `importsReady` — "נדרש עדכון מסד נתונים" ולא "לא יובאו קבצים"** — ה-`catch` שמגן על מסד לפני העדכון הופך "אין טבלה" ל"אין שורות", והרובריקה הציגה "עדיין לא יובאו קבצים" מול חשבון מלא בתנועות. זה בדיוק אותו שקר בתצוגה שהרובריקה נועדה לתקן. הבדיקה נעשית מפורשות (`bank_imports` + `bank_transactions.import_id`) והדף אומר מה חסר ומה ללחוץ. **כלל: כל `catch` שמכסה על סכימה חסרה חייב לבוא עם probe שאומר את זה בקול** — אחרת חוסר-עדכון נראה בדיוק כמו חוסר-נתונים.
- **🧹 ניקוי לפי קובץ** — `POST /reconciliation/purge-preview` (`requirePermission('import_bank')`) → אותו parser של הייבוא → **`matchRowsToTransactions`** → תצוגה מקדימה (כמה נמצאו, כמה יימחקו, כמה מותאמות ויידלגו, ודוגמת שורות) → אישור שולח את המזהים ל-`POST /reconciliation/txns/delete`. **זה הפתרון לקובץ שהועלה לחשבון הלא נכון לפני שמעקב הייבוא היה קיים**: לשורות אין `import_id`, הן נראות כמו כל תנועה, ואי אפשר לברור ביניהן ביד — אבל הקובץ עצמו אצל הבעלים והוא הסימן המדויק. ההתאמה היא **אותה שקילות של זיהוי כפילות בייבוא** (תאריך+סכום+תיאור+אסמכתה), וכל שורה בקובץ תופסת **תנועה אחת בלבד**, אחרת שתי שורות זהות היו מוחקות שלוש תנועות. הקובץ **אינו נשמר ואינו מיובא**. 🔴 שדה הקובץ חייב להיקרא `csv` — `multer` מוגדר `.single('csv')`, ושם אחר נדחה כ-unexpected field והבקשה נכשלת בשקט. `test/purge-by-file.test.js`.
- **תנועות שקדמו למעקב + מחיקה מרובה** — `untrackedSummary(accountId)` סופר תנועות עם `import_id IS NULL` ומציג הודעה עם טווח התאריכים; בלעדיה הרובריקה אמרה "עדיין לא יובאו קבצים" מול חשבון מלא בתנועות, וזה פשוט לא נכון. הן **אינן ניתנות לביטול כקובץ** (השיוך לא נשמר אז, ומחיקה גורפת הייתה מוחקת גם תנועות אמיתיות) — במקום זה יש **בחירה מרובה + "מחק נבחרות"** בטבלת "כל התנועות בחשבון" (`POST /reconciliation/txns/delete` → `deleteTransactions`), שעובדת יחד עם הסינון לכל עמודה: מסננים לשורות הזרות, "בחר הכל" מסמן **רק את הנראות**, ומוחקים. שורה מותאמת **מדולגת ונספרת**, לא מנותקת בשקט. 🔴 **הטופס עומד בפני עצמו ואינו עוטף את הטבלה** — לשורות יש טפסים משלהן, HTML אינו מרשה קינון, והדפדפן משטח אותם כך ש-`account_id` הוכפל וכפתורי השורה החלו לשלוח את הטופס החיצוני; הקישור נעשה ב-`form="bulk-del"` על תיבות הסימון. `test/bank-imports.test.js`.
- **🔴 היסטוריית ייבוא + ביטול ייבוא** — טבלת **`bank_imports`** (חשבון · מקור · שם הקובץ · נשמרו/כפילויות · מתי · מי) ו-**`bank_transactions.import_id`** על כל שורה. רובריקה "ייבואים אחרונים לחשבון זה" ב-`views/reconciliation/index.ejs`, ו-`POST /reconciliation/imports/:id/delete` (`requirePermission('import_bank')` + `assertInScope('bankAccount')`). **בלי זה לא הייתה שום דרך לדעת מה הועלה ולאיזה חשבון — ולכן גם לא לבטל**: קובץ של חנות אחת שנחת בחשבון של אחרת נשאר מעורבב בתנועות אמיתיות, וההתאמה האוטומטית סורקת אותו. הודעת הייבוא אומרת עכשיו את **שם הקובץ ואת שם החשבון** בקול. עמודת "מצב" עונה על "נשמר ומוכן להתאמה". **`deleteImport` מסרב כברירת מחדל אם חלק מהשורות כבר הותאמו** ומחזיר את מספרן; רק אישור שני (`release=1`, אחרי `confirm` שאומר כמה) משחרר את ההתאמות ומוחק. `test/bank-imports.test.js`.
- **ייבוא קובץ פעולות בחשבון (CSV/Excel) + הוספת תנועה ידנית** — שני כפתורים ב**כרטיס העליון**, ליד בורר החשבון ו"משוך תנועות מהבנק", ובנוסף במצב הריק של "כל התנועות בחשבון". שניהם פותחים את אותם `#csvDlg`/`#txnDlg` (`POST /reconciliation/import-csv`, `multipart/form-data`), ושניהם בתוך `can('import_bank')`. **קודם הם היו שני אייקונים עגולים בכרטיס בלי כותרת בתחתית הדף** — נמדד בדפדפן: 916px בדסקטופ ו-1355px בנייד, כלומר מתחת לקפל בשני המקרים, והבעלים לא מצא אותם. הכנסת התנועות היא הפעולה הראשונה בדף (בלי תנועות אין מה להתאים), ולכן היא בראש. `test/recon-import-visible.test.js`.
- **סינון ומיון בטבלת "תנועות לא מותאמות"** — `class="sortable filterable"` על הטבלה; עמודת "התאמה" מסומנת `data-nosort data-nofilter` (היא טפסים, לא טקסט). שני המשפרים גלובליים ב-`partials/footer.ejs`, ולכן **כל טבלה עתידית מקבלת אותם בהוספת class**. 🔴 שורת הסינון נבנית **בתוך** משפר "חיפוש בטבלה" הקיים ומזינה את אותה פונקציית `filter()` — שני מנגנוני הסתרה נפרדים על אותה טבלה נלחמים (אחד `style.display`, השני `hidden`), ותיבה שרוקנת לא הייתה מחזירה שורה שהשני הסתיר. `test/table-filters.test.js`.
- **הבאדג' "נפרע" מציג את אמצעי-התשלום האמיתי:** `listTransactions` מצרף `p.method AS matched_method` והתצוגה קוראת `methodLabel(t.matched_method)` (לא קשיח "צ׳ק").
- **משיכת תנועות ישירות מהבנק (בנקאות פתוחה — Financy / open-finance.ai)** — חלופה לייבוא ה-CSV: `POST /reconciliation/sync` (`requirePermission('import_bank')`, אותה הרשאה כמו הייבוא — אותו מעשה, אוטומטי) → `syncBankAccount` (`services/bankSync.js`) → `fetchFinancyTransactions` (`lib/financy.js`) → `mapFinancyTransactions` (`lib/financyMap.js`) → **`importTransactions(...,'financy',...)`** → **`autoReconcile`**. כלומר *אותו* צינור של ה-CSV, רק בלי הקובץ — מנוע ההתאמות, הסקופ וה-UI לא השתנו.
  - **קריאה בלבד:** רק `GET /data/accounts` ו-`GET /data/transactions` נקראים; ייזום תשלומים קיים ב-API של הספק ובמכוון **לא** מחווט. המפתח ב-**`FINANCY_API_KEY`** (env, Vercel; בלעדיו `enabled=false` והדף מציג "לא מוגדר" במקום להיכשל). דורש מנוי **Starter ומעלה** אצל Financy — בחינם ה-API מחזיר `NOT_AVAILABLE_ON_PLAN`.
  - **קישור חשבונות:** הגדרות ← 🏦 "קשר חשבונות בנק" (`POST /settings/financy-link`, `requireOwner`) → `linkFinancyAccounts` מתאים כל `bank_accounts` לחשבון אצל הספק לפי **סניף + מספר חשבון** (`parsedAccount`), כותב `bank_accounts.financy_account_id`. **התאמה דו-משמעית או חסרה = סירוב** (לעולם לא לנחש מאיזה חשבון בנק למשוך). חשבון לא-מקושר → הכפתור בדף התאמת בנק מוחלף בהסבר.
  - **חלון תאריכים חופף בכוונה** (`FINANCY_SYNC_DAYS`, ברירת מחדל 90): הבנק מפרסם תנועה ימים אחרי תאריך הערך. המשיכה החוזרת חינם כי **`bank_transactions.external_id`** (ה-`SK` של הספק) הוא מפתח הדדופ ב-`importTransactions` — שורה עם external_id מתדדפת עליו בלבד, ולכן שינוי תיאור/תאריך-ערך בין משיכות לא מייצר כפילות. שורות CSV/ידניות (בלי external_id) ממשיכות בדדופ הישן לפי שוויון שדות.
  - **מה מדולג במיפוי** (`lib/financyMap.js`, טהור ובדוק): `status` PENDING/DELETED/REJECTED, `isDuplicate` (אותו חיוב שנראה גם דרך הבנק וגם דרך המנפיק), שורות בלי תאריך/סכום ושורות בסכום 0. **הסימן עובר כמו שהוא** — `amount.chargedAmount.amount` כבר שלילי בחובה, בדיוק מה שמנוע ההתאמות מצפה לו. **`entryReference` = האסמכתא = מספר הצ׳ק** → זה מה שמאפשר התאמה דטרמיניסטית; בלעדיו נופלים ל-`code`. תאריך: `valueDate` → `bookingDate` → `transactionDate`. סכומים ב-`Math.round(x*100)` (11.7 ⇒ 1170, לא 1169).
  - **משיכה יומית אוטומטית:** `GET|POST /ingest/bank-sync` (ב-`routes/ingest.js`, מותקן **לפני** שער ההזדהות — לפינג מתוזמן אין משתמש מחובר) → `syncAllLinkedAccounts` עובר על כל חשבון מקושר. מאומת ב-**`CRON_SECRET`**: Vercel Cron שולח אותו לבד כ-`Authorization: Bearer $CRON_SECRET`, ומתקבל גם `?key=` / `x-cron-secret` לסקדולר אחר או ל-curl. **בלי `CRON_SECRET` → 503** (לעולם לא פתוח). התזמון ב-`vercel.json` → `crons`: `0 4 * * *` (≈07:00 בישראל, אחרי שהבנק פרסם את הלילה). **כשל בחשבון אחד לא עוצר את השאר** — כל חשבון ב-try/catch, החשבונות שהצליחו כבר נשמרו, והתשובה היא **207** על כשל חלקי (502 אם כולם נכשלו) + התראת טלגרם/פעמון. הרצה חוזרת בטוחה לחלוטין (חלון חופף + `external_id`).
- **ערוץ בנק שני: סריקה (`israeli-bank-scrapers`)** — לחשבונות שבנקאות פתוחה לא מכסה או לא מכסה בזול (כרטיסי אשראי, מכסת חשבונות). הצינור זהה: `POST /ingest/bank-txns` → `importScrapedBatch` (`services/bankSync.js`) → **`importTransactions(...,'scraper',...)`** → **`autoReconcile`**.
  - **הסריקה רצה מחוץ לאפליקציה** — `scripts/scrape-push.mjs` על **GitHub Actions** (`.github/workflows/bank-scrape.yml`, `20 4 * * *`, `concurrency: bank-scrape` כדי שלא ירוצו שתי סריקות במקביל) או על כל מכונה עם דפדפן. **Vercel לא יכולה להריץ אותה** — הספרייה מריצה Chromium אמיתי מול אתר הבנק. `israeli-bank-scrapers` היא **תלות אופציונלית** שנטענת lazily ומותקנת רק ברץ (`npm install --no-save`), כדי שהאפליקציה תתקין ותפרוס בלעדיה.
  - **🔴 פרטי הבנק לעולם לא מגיעים לאפליקציה** — הם יושבים רק ב-GitHub Secrets (`BANK_SCRAPERS` = JSON של `{companyId, credentials}`), הרץ מתחבר ושולח **שורות מוכנות** בלבד, כמו העלאת CSV. לא ב-Neon, לא ב-Vercel. שינוי הארכיטקטורה הזו = הכנסת סיסמאות בנק למסד.
  - **קליטה:** `POST /ingest/bank-txns` (ב-`routes/ingest.js`, לפני שער ההזדהות), מאומת ב-**`CRON_SECRET`** (Bearer / `?key=` / `x-cron-secret`); בלעדיו **503**. חשבונות מזוהים **לפי מספר חשבון בנק** (ספרות בלבד) — **מספר לא מוכר מדווח ב-`unmapped` ולא מנוחש** (תשובה **207**, התראה לבעלים), כך שפק"מ/חשבון רדום שנסרק בטעות לא נוחת בספר הלא נכון.
  - **מיפוי טהור** ב-`lib/scraperMap.js`: מדלג על `status !== 'completed'` (שורה ממתינה חוזרת אח"כ כ-completed עם מזהה משלה), על שורות בלי תאריך ועל סכום 0. `identifier` = האסמכתא = **מספר הצ׳ק**. `externalId` = **`scr:<companyId>:<accountNumber>:<identifier>`** — ממוען כדי שלעולם לא יתנגש ב-`SK` של Financy על אותו חשבון; בלי `identifier` (נפוץ בכרטיסי אשראי) הוא **null** ונופלים לדדופ לפי שוויון שדות — שמרני בכוונה (עלול למזג שני חיובים זהים באותו יום, לעולם לא ליצור כפילות).
  - `src/scraper/scrape.js` = מתאם רב-מוסדי (`SCRAPER_COMPANIES`: הפועלים/לאומי/דיסקונט/מזרחי/אוצה"ח/בינלאומי/מסד/יהב/יוניון + ישראכרט/אמקס/כאל/מקס). `src/scraper/hapoalim.js` נשאר כנקודת כניסה בשם הישן (מייצא מחדש את המפה) ו-`src/scraper/run.js` הוא ה-CLI המקומי.
  - **עמודות חדשות** (סכימה ×3 + `migrateOpenBankingSync`): `bank_accounts.financy_account_id`, `bank_transactions.external_id` + אינדקס ייחודי חלקי `ux_bank_txn_external(bank_account_id, external_id) WHERE external_id IS NOT NULL`. הסרת ה-external_id מחזירה כפילויות בכל משיכה חוזרת.
- **התראה: צ׳ק מבוטל שהופיע בבנק** — `voidedCheckHits(accountId)` מוצא חיוב בנק לא-מותאם שתואם תשלום **`voided`** (אותו סכום + מזהה הצ׳ק בטקסט התנועה). `autoReconcile` שולח על כך `notify` (טלגרם) ומחזיר `voidedSeen`; `voidedChecksSeenInBank(scope,storeId)` מזין דגל **אדום בלוח הבקרה** (`voidedInBank` ב-`routes/index.js`, באנר ב-`dashboard.ejs`). המשמעות: הכסף עבר למרות שהצ׳ק בוטל — לבדיקה.

---

## 👥 עובדים — `routes/employees.js` · 🧾 הפקדות — `services/deposits.js`
עובדים: `GET/POST /employees`, מחיקה. מפרעות/שכר מ-Z ניזונים לספר-מעקב. הפקדות ("הצהרות הפקדה") מותאמות לבנק לפי bag.

| פעולה | route | wiring | מה קורה | תלות/שבירה |
|------|-------|--------|---------|-------------|
| הוספת עובד (+טלפון) | `POST /employees` | `createEmployee` (`services/employees.js`) | שם פרטי/משפחה חובה; טלפון אופציונלי (`employees.phone`, נורמל ב-`normalizePhone`). בחירת עובד באפליקציה תמיד מרשימה מנוהלת (בוררי `cxEmpOptsFor`). | עמודת `employees.phone` (סכימה ב-3 מקומות + migrate). |
| **ייבוא עובדים מ-Excel/CSV** | `POST /employees/import` (multer memoryStorage, ≤5MB) | `parseEmployeeFile` (`lib/employeeImport.js`, `readXlsx`/CSV) → `importEmployees` | מזהה כותרות שם/טלפון (עברית+אנגלית), מפצל "שם מלא", מדלג שורות ללא שם. **דדופ לפי טלפון** — עובד שכבר קיים (או כפיל בקובץ) לא נוסף. הודעה: נוספו/כבר קיימים/דולגו. | דדופ ע"י `normalizePhone` (מקפל 972→0). שורה ללא טלפון תמיד נוספת (אין מפתח דדופ). |
- **הפקדה מפוצלת לכמה שקיות** — `POST /reports/zreports[/:id]` שולח שורות חוזרות (`dep_id[]`/`dep_bag[]`/`dep_amount[]` + `dep_deposited` שנושא את **אינדקס השורה** כערך, כי צ׳קבוקס כבוי אינו נשלח כלל) → `routes/reports.js#parseDepositRows` → **`services/deposits.js#replaceDepositsForZ`**. כפתור "➕ הוספת שקית הפקדה" ב-`_zform.ejs`; `depSum()` סוכם את כולן ל"סה"כ הופקד" ולחוסר/יתרה, ורשימת ה-Z סוכמת דרך `depositsForZ` ולא `depositForZ` (שקית ראשונה בלבד = חוסר מדומה). **🔴 שורה שכבר הותאמה לתנועת בנק (`matched_txn_id`) אינה נמחקת בעריכה** — זו עובדה שקרתה, ומחיקתה הייתה מוחקת גם את ההתאמה ואת `recon_diff` ומחזירה את התנועה ל"לא מותאמת". שורות מזוהות ב-`dep_id` ומעודכנות במקום. `upsertDepositForZ` **הוסרה** (מסלול כתיבה שני לאותם נתונים = סחיפה). `test/deposit-bags.test.js`. 
- **🔴 "חוסר / יתרה" בהצהרת הפקדה = הפקדה − (`drawer_cash` + הוצאות מזומן)** — `services/zreports.js#depositBase`/`depositDiff`, ומאותו חישוב חי ב-`views/reports/_zform.ejs#drawerCash()`. **לא `drawer_total`**: להפקדת מזומן אין קשר לאשראי/צ׳קים/הקפה/תווי קניה, והבסיס הישן הציג "חוסר" בגובה הכנסות האשראי כמעט בכל יום. ההוצאות מתווספות חזרה כי אותו מזומן נכנס לקופה ויצא ממנה לפני השקית. הנוסחה קיימת **פעמיים** (שרת + JS בדף) — `test/zdeposit-base.test.js` נועל את שתיהן, כולל בדיקת מקור על ה-EJS.
- **רובריקות "הוצאות במזומן" / "הכנסות מאשראי"** (`_zform.ejs`, `.zpanels`) — **זו לצד זו**: מזומן מימין, אשראי משמאל; בנייד אחת מתחת לשנייה. כל אחת `<details class="zpanel">`, כלומר **הכותרת כולה היא הכפתור** (שטח לחיצה גדול, נגיש במקלדת) במקום אייקון עגול לצדה — ולכן פתיחה מקוד היא `.open = true` ו**לא `.hidden`**, אחרת פתיחת פאנל האשראי בכשל התאמה לא עושה כלום. הרובריקה מציגה **סיכום בלבד** בצורת טבלת האשראי; ההזנה עברה ל-`<dialog id="cx-dialog">` (בתוך ה-`form`, ולכן השורות נשלחות כרגיל) עם אותו `.cx-list` שארבעת המסכים חולקים. הסיכום נבנה ב-JS מהשורות שבחלון ולא מהשרת — שורה שהוקלדה וטרם נשמרה חייבת להופיע. שתי הטבלאות מסומנות `no-enhance` (טבלאות הזנה — "חיפוש בטבלה + ייצוא" עליהן הוא רעש). `test/zform-panels.test.js`.
- **כפתור שמירה דביק לטופס ה-Z** (`.zform-save`, `views/reports/_zform.ejs` + `nocturne.css`) — הכפתור היה בתחתית טופס בן ארבעה חלקים ושני אקורדיונים מקופלים; מי שמילא את דוח המגירה למעלה לא ראה שיש בכלל מה ללחוץ.
- **מחזור חיי הפקדה** (`depositStatus(d)` — נגזר, ללא שינוי סכימה): `matched_txn_id`→**הותאמה בבנק**, `deposited=1`→**הופקדה**, אחרת→**הונפקה**. `reconcileDeposits` (`services/reconciliation.js`) מתאים **לפי מספר אסמכתה=שקית בלבד** (לא תלוי סכום; מתעד `recon_diff`=בנק−מוצהר). מוצג בבאדג' ב"היסטוריית הפקדות".
- **רובריקות** (דוחות-Z למטה + קוביות בלוח הבקרה): "דוחות Z ללא הצהרת הפקדה" (`zReportsWithoutDeposit`) → טופס הצהרה inline (`POST /reports/deposit-declare`); "הפקדה שהוצהרה ולא הופקדה" (`declaredNotDeposited`) → סימון הופקד (`POST /reports/deposits/:id/deposited`, מקבל `bag_number` אופציונלי). שתיהן מכובדות ל-`storeId` פעיל.
- **סריקת ברקוד שקית**: `public/bag-scan.js` — `BarcodeDetector` מובנה + מצלמה (`getUserMedia`) היכן שנתמך, אחרת נפילה להקלדה ידנית (CSP-safe, ללא ספרייה חיצונית). כפתור עם `data-bag-scan="<input-id>"`.

---

## ⚙️ הגדרות — `routes/settings.js`, `views/settings/*`, `partials/_ownerDialogs.ejs`

| כפתור/פעולה | route | wiring | מה עושה / מקושר | אם מוחקים/משנים/מזיזים |
|---|---|---|---|---|
| חברות/חנויות/חשבונות | `POST /settings/{companies,stores,accounts}[/…]` | `services/orgs.js` | הקמה + `deleteStore`. | חשבוניות/Z תלויים בחנות+חשבון בנק. |
| משתמשים + הרשאות + מטריצת חברות | `POST /settings/users[/:id/…]` | `services/users.js`, `_permpicker.ejs`, `lib/scope.js` | הרשאות ויזואליות (`PERMISSIONS`), presets (`ROLE_PRESETS`), הפרדת חברות. | הרשאות משפיעות על ה-firewall ועל הניווט. |
| מטריצת הרשאות חנויות (פר-חנות) | `POST /settings/users/:id/stores` (owner) | `storeGrantMatrix`/`setUserStores` (`lib/scope.js`), עמודות מ-`listStructure` (חברה→חנויות) | סימון חנויות → `user_stores`; ריק = כל חנויות החברות המורשות. קובע את `availableStores` (בורר החנות הפעילה). | מוגן ב-`router.use('/users', requireOwner)`. תלוי ב-`companies` (nested stores) ו-`storeGrants` מה-render. |
| הזמנה / קישור הגדרה-עצמית | `POST /settings/users/:id/{invite,invite-link}` | `services/passwordReset.js` (`createInviteLink`,`completeInvite`) | וואטסאפ / קישור `/invite/:token`; מדיניות סיסמה ≥6, אות גדולה+קטנה+ספרה, אלפאנומרי. | טוקנים ב-`password_resets`. מדיניות ב-`lib/auth.js`. |
| **עדכון מסד נתונים** (`dlg-db`) | `POST /settings/db-upgrade` | `upgradeSchema` (re-apply `schema.pg.sql`) | הרצה אחרי כל שינוי סכימה. בטוח, לא מוחק. | חובה אחרי עמודה/טבלה חדשה. |
| גיבוי מלא (`dlg-backup`) | `GET /settings/backup` | `exportAll` (`backup.js`) | JSON snapshot של כל הטבלאות. | — |
| שחזור (`dlg-restore`) | `POST /settings/restore` | `restoreAll` | מחליף הכל. בלתי הפיך. מוגן בסיסמת בעלים. | — |
| **התחלה נקייה** (`dlg-clean`) | `POST /settings/clean-start` | `cleanStartInvoicesPaymentsZ` | מוחק חשבוניות/תשלומים/דוחות-Z/טיוטות; **שומר** z_closings/ספקים/קטלוג/עובדים/הפקדות. מנתק FK מתים קודם, מוחק child→parent. מוגן בסיסמה. | בלתי הפיך. שינוי הסדר = הפרת FK ב-PG. |
| **איפוס** (`dlg-reset`) | `POST /settings/reset-data` | `resetTransactionalData` | מוחק נתוני עבודה, שומר הקמה. בלתי הפיך. | — |
| **צילום חשבוניות — נעול/פעיל** | `POST /settings/scan-toggle` | `setScanEnabled` (`appSettings.js`) | מפעיל/נועל את הצילום (`app_settings.scan_enabled`). | ראה "נעילת צילום". |
| בדיקת אחסון | `POST /settings/storage-test` | `lib/storage.js` | כתיבה/קריאה/מחיקה של קובץ זעיר. | — |
| ~~טעינת קטלוג-על~~ | `POST /settings/catalog-import[/…]` | `masterCatalog.js` | **הוסתר מה-UI** (הכפתור+הדיאלוג הוסרו). ה-route קיים אך לא מקושר. | ראה "מוצרים/קטלוג מוסתרים". |

> `_ownerDialogs.ejs`: כל כפתור `apOpen('dlg-X')` דורש `<dialog id="dlg-X">` באותו קובץ — מבחן אוכף. `GET` על נתיב-פעולה מפנה ל-`/settings` (רשימת ה-redirect ב-סוף `settings.js`) — הוסף נתיב חדש גם לשם.

---

## 📷 נעילת צילום + מוצרים/קטלוג מוסתרים

**נעילת צילום (entitlement):**
- מקור אמת: `app_settings.scan_enabled` (`'0'` נעול = ברירת מחדל) דרך `services/appSettings.js` (`isScanEnabled`/`setScanEnabled`).
- **שער** ב-`app.js`: `app.use('/scan', requirePageAccess('nav_scan'), scanGate, scanRoutes)` — כשנעול מרנדר `views/scan-locked.ejs` (423) ל-GET, ומחזיר 423 לשאר.
- `res.locals.scanEnabled` נקבע ב-`app.js` (middleware) עבור ה-nav (🔒).
- **אם משנים:** ברירת המחדל נעול — לקוח חדש לא רואה צילום עד הפעלה. **מבחנים שנוגעים ל-`/scan` חייבים `setScanEnabled(true, db)` ב-setup** (`test/scan-routes.test.js`). בסיס לחבילת-צילום עתידית (X חשבוניות + חיוב לכל נוספת).

**מוצרים/קטלוג-על מוסתרים:**
- לינק "מוצרים" הוסר מ-`header.ejs`; דיאלוג "טעינת קטלוג-על" הוסר מ-`_ownerDialogs.ejs`.
- ה-route `/products` ו-`masterCatalog.js`/`catalogFile.js` **נשארו מותקנים** — מנוע הצילום עדיין משתמש בקטלוג, וזה קו-עבודה מקביל. **אל תמחק את קוד הקטלוג** בלי לתאם.

---

## 🔐 Auth, הרשאות ו-scope (קריטי ל-firewall)

- `middleware/currentUser.js` — שער אימות: קורא cookie חתום, טוען `req.user`, קובע `res.locals.can/canView`, `req.scope`. אוכף `must_change_password` ושעות-התחברות. נתיבים ציבוריים: `/login,/forgot,/reset,/invite,/privacy,/accessibility`.
- `lib/permissions.js` — `PERMISSIONS` (קטלוג), `userCan`/`canViewPage` (בעלים=תמיד true), `NAV_PAGES`/`NAV_ALLOW`/`OPEN_PATHS`, `ROLE_PRESETS`, `firstAllowedPath`.
- `middleware/requireOwner.js` — `requireOwner`,`requirePermission(key)`,`requirePageAccess(nav_key)`, ו-**`enforcePageScope`** (default-deny לפני ה-routes).
- הפרדת חברות **+ חנות** (נאכפת): `lib/scope.js` — `scopeClause(scope,col)` (חברה; **סובל אובייקט req.scope**, לוקח `companyIds`), **`scopeWhere(scope,companyCol,storeCol)`** (חברה+חנות), `normalizeScope` (מקבל מערך/null או `{companyIds,storeIds}`). `req.scope={companyIds,storeIds}`. שירותי-list קוראים `scopeWhere` וה-routes מעבירים `req.scope` → משתמש עם הרשאת-חנות מפורשת רואה רק את חנויותיו; הרשאת-חברה בלבד → `storeIds`=כל חנויות החברה (על-קבוצה, לא מצמצם).
- IDOR לפי-id: `lib/scopeGuard.js` — `assertInScope(kind,id,scope)`/`scopeParam` פותרים `{company_id,store_id}` לכל kind (`SCOPE_OF`: `invoice,payment,zreport,deposit,expense,scanDraft,bankAccount,bankTxn`) ודוחים (404) מחוץ-לחברה **או** מחוץ-לחנות. `scope` יכול להיות מערך companyIds (חברה בלבד, תאימות) או אובייקט req.scope (חברה+חנות). שורה ללא store_id נשארת גלויה בתוך החברה. **יצירה בשירות אינה מסוקפת** (כמו `createInvoice`/`createPayment`) — הסקופ נאכף ב-route/list/read; טופס חשבונית/Z ננעל לחנות הפעילה. פקידה: כמה בוררים בלבד (dropdown חנויות ב-formData/scan, קבוצת חשבונות ב-auto-reconcile) עדיין ברמת-חברה — מכוסים ע"י נעילת החנות הפעילה.
- **אם משנים:** נתיב חדש שדורש גישה מוגבלת → הוסף ל-`NAV_ALLOW`; ציבורי → `OPEN_PATHS` + `currentUser.js`. שינוי `requirePageAccess` במאונט = פותח/סוגר דף שלם.

---

**דוח אשראי ב-Z:** `CC_BRANDS` (`services/zreports.js`) הוא **גם סדר התצוגה** — `_zform.ejs` מרנדר אותו כמו שהוא. הסדר לפי הבעלים: ישראכרט, כ.א.ל, דיינרס, אמ. אקס, **לאומיק.**, כרטיס תייר, כללי. הוספת מותג = עמודת `cc_<key>` בסכימה ×3 + migrate **וגם** ב-`UPDATE` של `setCreditCards` (אחרת הסכום נשמר חלקית).

## חוקי-על עסקיים (R-rules)
- **R1:** תשלום רק לספק+חשבונית מאושרים.
- **R2:** מספר הקצאה כפול = חסימה קשה; ספק+מספר כפול = אזהרה רכה.
- **R3:** חשבונית מס מעל סף בלי הקצאה = on_hold. נבדק **סימטרית** גם ב-`updateInvoice` (מציב החזקה כשעריכה חוצה את הסף, מנקה כשלא) — לא רק ביצירה.
- **R5:** סכום תשלום = Σ שורות מיושמות (net; זיכוי שלילי).
שינוי כל אחד מאלה משפיע על `createInvoice`/`createPayment`/`updatePayment` — ראה `services/invoices.js` + `services/payments.js`.
- **מניעת תשלום-כפול (מרוץ/double-submit):** ב-`createPayment` המעבר ל-`status='paid'` הוא UPDATE **מותנה** — `WHERE id=? AND status IN ('recorded','approved_for_payment')`; אם `changes===0` (החשבונית כבר שולמה/שונתה בו-זמנית) → זריקת שגיאה וגלגול-לאחור של כל התשלום. ב-Postgres (READ COMMITTED) העסקה השנייה ננעלת על השורה עד ה-COMMIT הראשון ואז רואה `paid` → 0 שורות. SQLite מסדרן כותבים ממילא. **אל תחליף חזרה ל-UPDATE ללא-תנאי** — זה מחזיר את חלון המרוץ.

---

## ✅ צ'קליסט לפני דחיפה
1. קראתי את סעיף האינווריאנטים + הפיצ'ר בטבלה.
2. סכימה? עדכנתי `schema.sql` + `migrate.js` + `schema.pg.sql`, וכל `SELECT` מפורש.
3. `npm test` ✔ + `TEST_PG=1 npm test` ✔ + `node scripts/smoke.mjs` ✔. (`test/data-flow.test.js` מאמת את **חיבורי הנתונים בין המסכים** — Z/סגירה/חשבונית/תשלום/בנק/הפקדה/ספק/עובד → יעדיהם; אם שינית שאילתת מקור, ודא שלא נשבר.)
4. `git fetch apnew main && git rebase apnew/main`; קידמתי `BUILD_VERSION` (מספר טרי).
5. דחפתי ל-`apnew ap-control-split:main` + `origin ap-control-split`; אימתתי footer חי.
6. סכימה? הזכרתי לבעלים ללחוץ **"עדכן מסד נתונים"**.
7. עדכנתי `INDEX.md` + `CLAUDE.md`.
