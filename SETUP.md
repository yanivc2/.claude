# מדריך הפעלה — AP Control

מדריך מעשי להתקנה, הרצה ותפעול יומיומי. ההסברים בעברית; פקודות ההרצה מותאמות ל-Windows +
PowerShell (סביבת העבודה הראשית), אך זהות כמעט לחלוטין בכל מערכת.

---

## 1. התקנה והרצה ראשונה

### דרישה מוקדמת
- **Node.js גרסה 22.12 ומעלה** (בדוק: `node --version`). אם חסר — התקן מ-https://nodejs.org
  (גרסת LTS עדכנית).

### שלבים (PowerShell)
```powershell
# היכנס לתיקיית הפרויקט (בתוך ה-repo)
cd ap-control

# התקנת תלויות (פעם אחת)
npm install

# יצירת קובץ הגדרות מקומי מהתבנית
Copy-Item .env.example .env

# הפעלה
npm start
```
בהצלחה תראה: `AP Control פועל: http://localhost:3000`

פתח בדפדפן: **http://localhost:3000**

### גישה ממחשבים אחרים במשרד (הרשת המקומית)
המזכירה ניגשת מהדפדפן שלה אל: `http://<כתובת-ה-IP-של-המחשב>:3000`
- מצא את כתובת ה-IP: `ipconfig` (חפש IPv4 Address, למשל `192.168.1.20`).
- ייתכן שתצטרך לאשר את הפורט ב-Windows Firewall בפעם הראשונה.

### עצירה / הפעלה מחדש
- עצירה: `Ctrl+C` בחלון ה-PowerShell.
- כדי שהמערכת תרוץ תמיד ברקע — ראה §6 (הרצה קבועה).

---

## 2. הרצה ראשונה — מה לבדוק מיד

1. בראש העמוד, החלף לתפקיד **בעלים** (בורר המשתמש מימין).
2. היכנס ל**הגדרות** → ודא ש-4 החנויות, 4 החשבונות ו-3 החברות קיימים ונכונים
   (מגיעים כ-seed). מספרי הח.פ. כבר מוזנים.
3. אם צריך להוסיף חנות/חשבון/חברה — עשה זאת כאן (ראה §4).

> **בורר התפקיד אינו אבטחה** — הוא רק מפריד תפקידים (בעלים/מזכירה) בשלב הנוכחי. המערכת רצה
> מקומית ברשת המשרד. אם בעתיד תיחשף מעבר לרשת — צריך להוסיף אימות אמיתי.

---

## 3. זרימות עבודה יומיומיות

### מזכירה — הזנת חשבונית
1. **חשבוניות → + חשבונית חדשה**.
2. בחר ספק. אם חדש → **+ פתח ספק חדש** (נפתח כ"ממתין"; אפשר להמשיך להזין, אך תשלום חסום עד
   אישור בעלים).
3. מלא: חנות, מספר חשבונית, מספר הקצאה (אם יש, 9 ספרות), תאריך, סוג, סכומים.
4. אם קופצות **אזהרות כפילות** — בדוק; אם זו באמת חשבונית חדשה, אשר עם סיבה.
5. שמור. *(אפשר לצרף תמונת סריקה/צילום — שלב 1b.)*

### מזכירה — תשלום בצ׳ק
1. אשר את החשבונית לתשלום (בעמוד החשבונית → **אשר לתשלום**).
2. **צ׳קים → + צ׳ק חדש** → בחר את החשבוניות (מאותו חשבון בנק) + זיכויים → המערכת מחשבת נטו.
3. כתוב את הצ׳ק הפיזי ידנית, הקלד את **מספר הצ׳ק** → הנפק. *(אין הנפקה ללא חשבונית מאושרת
   ומספר צ׳ק, וסכום הצ׳ק = סכום השורות.)*

### בעלים
- **ספקים** → אשר/חסום ספקים ממתינים (פעולת בעלים בלבד).
- **חשבוניות** → סקור חשבוניות "מוחזקות" (למשל חשבונית מס מעל 5,000 ₪ ללא מספר הקצאה) ושחרר
  או השלם הקצאה.
- **ביטול צ׳ק** (למשל צ׳ק שנפסל) — בעלים בלבד.

### התאמת בנק (פירעון צ׳קים)
- **התאמת בנק** → בחר חשבון → **ייבא CSV** של תנועות הבנק (או הוסף ידנית) → **התאמה
  אוטומטית**. צ׳ק שנפרע יסומן אוטומטית ויירד מ"צ׳קים בחוץ". במקרה של שני צ׳קים באותו
  סכום — תתבקש להכריע.

### דוחות
- **צ׳קים בחוץ** — כמה כסף "בחוץ" פר חשבון.
- **בדיקת חשבונית** — חיפוש מהיר לפי מספר/הקצאה/ספק.
- **רווחיות** — הזן מכירות (Z מהקופות) ידנית; קניות מחושבות אוטומטית → רווח גולמי.
- כל דוח ניתן לייצוא **CSV** או **הדפסה/PDF** (כפתורים בראש הדוח).

---

## 4. הוספת חנות / חשבון / חברה (בעלים)

**הגדרות** (בתפריט, לבעלים בלבד):
- **חברה חדשה** — שם + ח.פ.
- **חנות + חשבון בנק חדשים** — בחר חברה, שם חנות, בנק/סניף/מספר חשבון (יחס 1:1).
- עריכה של ח.פ. / שם תצוגה — במקום.

---

## 5. גיבוי — חובה (§12)

קובץ ה-SQLite (`data/`) ותיקיית התמונות (`uploads/`) הם **נקודת כשל יחידה**. הגדר **גיבוי
יומי אוטומטי אל מחוץ למחשב** (כונן חיצוני / ענן).

דוגמה: סקריפט PowerShell לגיבוי יומי (`backup-ap.ps1`):
```powershell
$src = "C:\path\to\ap-control"
$dst = "D:\Backups\ap-control"   # כונן חיצוני / תיקיית ענן מסונכרנת
$stamp = Get-Date -Format "yyyy-MM-dd"
$target = Join-Path $dst $stamp
New-Item -ItemType Directory -Force -Path $target | Out-Null
Copy-Item (Join-Path $src "data")    (Join-Path $target "data")    -Recurse -Force
Copy-Item (Join-Path $src "uploads") (Join-Path $target "uploads") -Recurse -Force
# שמור גיבויים של 30 הימים האחרונים בלבד
Get-ChildItem $dst | Where-Object { $_.CreationTime -lt (Get-Date).AddDays(-30) } | Remove-Item -Recurse -Force
```
הפעל אוטומטית יומית עם **Task Scheduler** (Create Basic Task → Daily → Start a program:
`powershell.exe -File C:\path\to\backup-ap.ps1`).

---

## 6. הרצה קבועה ברקע (מומלץ)

כדי שהמערכת תרוץ תמיד (גם אחרי אתחול), אפשרות פשוטה: Task Scheduler עם טריגר "At startup"
שמריץ `npm start` בתיקיית הפרויקט. אפשרות מתקדמת: כלי כמו `pm2` או שירות Windows.

---

## 7. המשימות החיצוניות שנותרו 🔴 (טעונות פעולה שלך)

1. **גיבוי** (§5) — הכי דחוף. הגדר לפני שמזינים נתונים אמיתיים.

2. **סורק הבנק (שלב 2, §11.1–11.2)** — התאמת בנק אוטומטית מלאה:
   ```powershell
   npm install israeli-bank-scrapers
   # ערוך .env והוסף:  BANK_HAPOALIM_USER_CODE=...  BANK_HAPOALIM_PASSWORD=...
   npm run scrape          # מושך את השנה האחרונה
   ```
   🔴 **לאמת מול חשבון אמיתי:** (א) שהכניסה עובדת ללא OTP אינטראקטיבי; (ב) האם מספר הצ׳ק מופיע
   בתנועות. מנוע ההתאמה עובד גם בלי מספר הצ׳ק — עד לאימות, נתיב ה-CSV/ידני מלא ותקין.

3. **OCR (שלב 3, אופציונלי)** — קריאה מתמונה והשוואה למוקלד:
   ```powershell
   npm install tesseract.js
   ```
   ואז כפתור "הרץ OCR" בעמוד חשבונית עם תמונה. 🔴 דיוק בעברית מוגבל — התוצאה היא הצעה לאימות.

4. **הדפסת צ׳קים (שלב 4, §11.5)** — כרגע כל צ׳ק מודפס כ**טיוטה ("אינו סחיר")** ושורת ה-MICR
   היא placeholder. לפני הפקה אמיתית: (א) אישור הבנק להדפסה עצמית; (ב) התקנת גופן MICR (E-13B)
   מאושר; (ג) הגדר `CHECK_PRINTING_APPROVED=true` ב-.env. אפשר לקחת את הטיוטה לבנק לאישור.

---

## 8. פקודות שימושיות

| פקודה | מה עושה |
|---|---|
| `npm start` | הפעלת המערכת |
| `npm test` | הרצת כל הבדיקות |
| `npm run seed` | טעינת seed (בטוח להרצה חוזרת; לא דורס) |
| `npm run reset-db` | **מחיקה** ובנייה מחדש של בסיס הנתונים (זהירות!) |
| `npm run scrape` | משיכת תנועות בנק (דורש התקנה + credentials) |

## 9. פתרון תקלות מהיר
- **הפורט תפוס** — שנה `PORT` ב-.env.
- **"no space left"** — פנה מקום; אל תמחק את `data/`.
- **המזכירה לא מצליחה להתחבר** — ודא IP נכון ואישור ב-Firewall (§1).
- שגיאה ב-`.env`? המערכת רצה גם בלי `.env` (ברירות מחדל) — אך מומלץ ליצור אותו.
